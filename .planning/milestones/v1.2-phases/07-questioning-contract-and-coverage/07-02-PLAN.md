---
phase: 07-questioning-contract-and-coverage
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - templates/commands/specify.md
  - tests/test_alignment_templates.py
autonomous: true
requirements: [FDEP-01, FDEP-02]
user_setup: []
must_haves:
  truths:
    - "`sp-specify` follow-up questions build directly on the user's previous answer."
    - "`sp-specify` challenges vague, shallow, or contradictory answers with targeted next questions instead of generic resets."
    - "Phase 7 contract tests fail if answer-aware follow-up or contradiction handling drifts."
  artifacts:
    - path: "templates/commands/specify.md"
      provides: "The clarification-loop contract for stronger follow-up behavior."
    - path: "tests/test_alignment_templates.py"
      provides: "Regression coverage for answer-aware follow-up and contradiction handling."
  key_links:
    - from: "templates/commands/specify.md"
      to: "tests/test_alignment_templates.py"
      via: "Contract assertions for follow-up depth and contradiction handling."
    - from: "templates/commands/specify.md"
      to: ".planning/phases/07-questioning-contract-and-coverage/07-RESEARCH.md"
      via: "Phase research requirement that follow-up quality improve without becoming freeform brainstorming."
---

<objective>
Strengthen the `sp-specify` clarification-loop contract so follow-up questions build on the user's most recent answer, handle vague or contradictory input more aggressively, and remain structured enough for later experience and sync phases to build on.

Purpose: Turn the Phase 7 contract into something that supports deeper questioning, not just broader checklists.
Output: Updated follow-up rules in `templates/commands/specify.md` plus regression tests that guard FDEP-01 and FDEP-02.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-questioning-contract-and-coverage/07-RESEARCH.md
@templates/commands/specify.md
@tests/test_alignment_templates.py
@.agents/skills/sp-specify/SKILL.md
</context>

<threat_model>
<trust_boundaries>
- User answer -> clarification loop contract -> next question selection
- Shared `specify` template -> regression tests protecting follow-up behavior
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>P7-02-T1</threat_id>
    <category>Tampering</category>
    <component>clarification loop in templates/commands/specify.md</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Require explicit contract language that follow-up questions build on the user's previous answer and challenge vague or contradictory responses with targeted narrowing prompts.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>P7-02-T2</threat_id>
    <category>Information Disclosure</category>
    <component>questioning strategy scope</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep the contract structured and scoped to Phase 7 so it does not drift into a freeform brainstorming workflow or full shipped-surface sync work.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the clarification loop for answer-aware follow-up</name>
  <files>templates/commands/specify.md</files>
  <read_first>templates/commands/specify.md, .planning/REQUIREMENTS.md, .planning/phases/07-questioning-contract-and-coverage/07-RESEARCH.md, E:/work/github/superpowers/skills/brainstorming/SKILL.md</read_first>
  <action>
    Update the clarification-loop instructions so they explicitly require the next question to build on the user's most recent answer, not reset to generic prompts. Preserve the one-question-at-a-time structure and the current open-question-block format, but strengthen the wording around answer-aware follow-up, targeted narrowing, and when to avoid generic recap churn. Do not convert `specify` into a freeform brainstorming workflow.
  </action>
  <verify>rg -n "previous answer|generic prompts|one question at a time|clarification loop|vague|contradict" templates/commands/specify.md</verify>
  <acceptance_criteria>
    - `templates/commands/specify.md` contains explicit language that follow-up questions build on the user's previous answer.
    - `templates/commands/specify.md` keeps the one-question-at-a-time structure and does not add freeform brainstorming as the default mode.
  </acceptance_criteria>
  <done>The clarification loop now requires answer-aware follow-up instead of generic resets.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen contradiction and shallow-answer handling</name>
  <files>templates/commands/specify.md</files>
  <read_first>templates/commands/specify.md, .planning/REQUIREMENTS.md, .planning/phases/07-questioning-contract-and-coverage/07-RESEARCH.md</read_first>
  <action>
    Tighten the clarification rules so vague, shallow, or contradictory answers must trigger targeted next questions, examples, or recommendation framing that narrow the ambiguity. Keep the contract focused on high-impact ambiguity that matters for planning. Add or refine exact wording that prevents the workflow from accepting contradictions just because the user has already spoken at length.
  </action>
  <verify>rg -n "vague|shallow|contradict|targeted|recommendation|ambiguity" templates/commands/specify.md</verify>
  <acceptance_criteria>
    - `templates/commands/specify.md` explicitly mentions vague, shallow, or contradictory answers as triggers for targeted follow-up.
    - `templates/commands/specify.md` ties the stronger follow-up behavior to planning-relevant ambiguity rather than generic conversation depth.
  </acceptance_criteria>
  <done>The contract now specifies how `sp-specify` handles weak or contradictory user answers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression coverage for answer-aware follow-up and contradiction handling</name>
  <files>tests/test_alignment_templates.py</files>
  <read_first>tests/test_alignment_templates.py, templates/commands/specify.md, .planning/REQUIREMENTS.md</read_first>
  <action>
    Extend `tests/test_alignment_templates.py` so the `specify` contract test asserts explicit answer-aware follow-up language, explicit handling of vague/shallow/contradictory answers, and guardrails that keep the clarification loop structured instead of freeform. Keep the new assertions concrete and phrase-based so later contract drift is caught early.
  </action>
  <verify>pytest tests/test_alignment_templates.py -q</verify>
  <acceptance_criteria>
    - `tests/test_alignment_templates.py` asserts answer-aware follow-up language in `templates/commands/specify.md`.
    - `tests/test_alignment_templates.py` asserts explicit handling for vague, shallow, or contradictory answers.
    - `pytest tests/test_alignment_templates.py -q` exits with code 0.
  </acceptance_criteria>
  <done>The regression suite now protects FDEP-01 and FDEP-02 from silent drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "previous answer|generic prompts|vague|shallow|contradict" templates/commands/specify.md` returns matches.
- [ ] `pytest tests/test_alignment_templates.py -q` passes.
- [ ] The clarification loop remains one-question-at-a-time and structured.
</verification>

<success_criteria>
- All tasks completed
- The `specify` contract explicitly requires answer-aware follow-up
- The contract explicitly handles vague, shallow, and contradictory answers
- Regression tests protect FDEP-01 and FDEP-02
</success_criteria>

<output>
After completion, create `.planning/phases/07-questioning-contract-and-coverage/07-02-SUMMARY.md`
</output>
