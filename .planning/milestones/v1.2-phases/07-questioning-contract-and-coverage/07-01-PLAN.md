---
phase: 07-questioning-contract-and-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/commands/specify.md
  - tests/test_alignment_templates.py
autonomous: true
requirements: [QCOV-01, QCOV-02, QCOV-03]
user_setup: []
must_haves:
  truths:
    - "`sp-specify` asks planning-critical docs/config/process-change questions before releasing alignment."
    - "`sp-specify` uses task classification to choose which requirement dimensions to probe."
    - "`sp-specify` keeps clarifying when planning-critical ambiguity remains instead of exiting early."
  artifacts:
    - path: "templates/commands/specify.md"
      provides: "The authoritative questioning contract for `sp-specify`."
    - path: "tests/test_alignment_templates.py"
      provides: "Regression coverage for task-type gates and the planning-ready release gate."
  key_links:
    - from: "templates/commands/specify.md"
      to: "tests/test_alignment_templates.py"
      via: "Exact contract assertions for task-type questioning and release-gate behavior."
    - from: "templates/commands/specify.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 7 requirement coverage for QCOV-01, QCOV-02, and QCOV-03."
---

<objective>
Strengthen the `sp-specify` questioning contract so Phase 7 explicitly covers the right requirement dimensions for docs/config/process-change work, adapts the questioning path by task classification, and blocks early release when planning-critical ambiguity remains.

Purpose: Make the shared `sp-specify` contract better at surfacing the information planning actually needs.
Output: Updated `specify` template contract and regression tests for question coverage and release-gate behavior.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-questioning-contract-and-coverage/07-RESEARCH.md
@AGENTS.md
@templates/commands/specify.md
@tests/test_alignment_templates.py
@docs/superpowers/specs/2026-04-11-specify-analysis-rework-design.md
</context>

<threat_model>
<trust_boundaries>
- User request text -> `templates/commands/specify.md` questioning contract -> generated `spec.md` and `alignment.md`
- Shared template behavior -> regression assertions in `tests/test_alignment_templates.py`
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>P7-01-T1</threat_id>
    <category>Tampering</category>
    <component>templates/commands/specify.md</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Anchor Phase 7 changes to explicit task-type gates and release-gate assertions so superficial prompt churn cannot silently weaken requirement coverage.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>P7-01-T2</threat_id>
    <category>Repudiation</category>
    <component>planning-ready release decision</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Require explicit template language and tests proving that planning-critical ambiguity blocks normal completion unless the user explicitly force proceeds.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Tighten docs/config/process-change requirement coverage in `specify`</name>
  <files>templates/commands/specify.md</files>
  <read_first>templates/commands/specify.md, .planning/REQUIREMENTS.md, .planning/phases/07-questioning-contract-and-coverage/07-RESEARCH.md, AGENTS.md</read_first>
  <action>
    Update the `specify` outline so the docs/config/process-change path is treated as a planning-critical questioning surface rather than a passive list. Preserve the existing task-type section, but make the contract explicitly require the workflow to collect all five dimensions from `QCOV-01`: changed artifact, change objective, affected users or teams, compatibility/process constraints, validation method, and completion criteria before normal alignment release. Keep the current `specify -> plan` mainline language intact and avoid routing work into `clarify` or `spec-extend`.
  </action>
  <verify>rg -n "Docs/config/process change:|changed artifact|change objective|affected users or teams|compatibility/process constraints|validation method|completion criteria" templates/commands/specify.md</verify>
  <acceptance_criteria>
    - `templates/commands/specify.md` contains the `Docs/config/process change:` gate with all six planning-critical prompts listed.
    - `templates/commands/specify.md` keeps `specify -> plan` as the mainline and does not add any language making `clarify` mandatory.
  </acceptance_criteria>
  <done>The docs/config/process-change questioning contract is explicit and planning-facing.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen task-classification branching and ambiguity release rules</name>
  <files>templates/commands/specify.md</files>
  <read_first>templates/commands/specify.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/07-questioning-contract-and-coverage/07-RESEARCH.md</read_first>
  <action>
    Rewrite the classification and release-gate instructions so they clearly enforce `QCOV-02` and `QCOV-03`. The template must say that task classification changes the requirement dimensions being probed, that high-impact ambiguity around scope, workflow behavior, constraints, or success criteria keeps the workflow in clarification, and that `Aligned: ready for plan` is only valid when planning-critical ambiguity is resolved. Preserve the existing `Force proceed with known risks` path for explicit user overrides only.
  </action>
  <verify>rg -n "task classification|Docs/config/process change|planning-critical ambiguity|Aligned: ready for plan|Force proceed with known risks" templates/commands/specify.md</verify>
  <acceptance_criteria>
    - `templates/commands/specify.md` states that task classification changes which requirement dimensions are probed.
    - `templates/commands/specify.md` states that planning-critical ambiguity keeps the workflow in clarification until resolved or explicitly force-proceeded.
    - `templates/commands/specify.md` still includes both `Aligned: ready for plan` and `Force proceed with known risks`.
  </acceptance_criteria>
  <done>The release gate and classification-aware coverage rules now match the Phase 7 roadmap contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression assertions for task-type coverage and release-gate behavior</name>
  <files>tests/test_alignment_templates.py</files>
  <read_first>tests/test_alignment_templates.py, templates/commands/specify.md, .planning/REQUIREMENTS.md</read_first>
  <action>
    Extend `tests/test_alignment_templates.py` so the `specify` contract test asserts the presence of the docs/config/process-change gate, the required task-type-specific prompts from `QCOV-01`, classification-aware questioning language, and the rule that planning-critical ambiguity blocks normal alignment release unless the user explicitly chooses `Force proceed with known risks`. Keep the assertions in the existing contract-test style instead of adding snapshots.
  </action>
  <verify>pytest tests/test_alignment_templates.py -q</verify>
  <acceptance_criteria>
    - `tests/test_alignment_templates.py` asserts the docs/config/process-change gate and its planning-critical prompts.
    - `tests/test_alignment_templates.py` asserts task-classification-aware questioning or equivalent contract wording.
    - `pytest tests/test_alignment_templates.py -q` exits with code 0.
  </acceptance_criteria>
  <done>The regression suite now fails if Phase 7 coverage and release-gate rules drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Docs/config/process change:|change objective|affected users or teams|compatibility/process constraints|validation method|completion criteria" templates/commands/specify.md` returns matches.
- [ ] `rg -n "Aligned: ready for plan|Force proceed with known risks|task classification" templates/commands/specify.md` returns matches.
- [ ] `pytest tests/test_alignment_templates.py -q` passes.
</verification>

<success_criteria>
- All tasks completed
- The `specify` contract explicitly covers task-type questioning and release-gate behavior for Phase 7
- Regression tests protect QCOV-01, QCOV-02, and QCOV-03
- No `clarify`-as-mainline wording is reintroduced
</success_criteria>

<output>
After completion, create `.planning/phases/07-questioning-contract-and-coverage/07-01-SUMMARY.md`
</output>
