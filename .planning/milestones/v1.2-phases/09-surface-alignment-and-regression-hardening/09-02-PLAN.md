---
phase: 09-surface-alignment-and-regression-hardening
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - docs/quickstart.md
  - tests/test_specify_guidance_docs.py
autonomous: true
requirements: [SYNC-02, SYNC-03]
user_setup: []
must_haves:
  truths:
    - "User-facing workflow guidance teaches `specify -> plan` as the mainline."
    - "Compatibility guidance for `clarify` remains available without restoring it as the recommended main path."
    - "Regression tests fail if shipped guidance drifts back toward `specify -> clarify -> plan`."
  artifacts:
    - path: "docs/quickstart.md"
      provides: "Updated release-facing workflow guidance."
    - path: "tests/test_specify_guidance_docs.py"
      provides: "Regression coverage for workflow-guidance truthfulness."
  key_links:
    - from: "docs/quickstart.md"
      to: "README.md"
      via: "The quickstart should teach the same mainline workflow as the primary repo docs."
    - from: "docs/quickstart.md"
      to: "tests/test_specify_guidance_docs.py"
      via: "Focused doc assertions for mainline and compatibility-only guidance."
---

<objective>
Update release-facing guidance and add regression checks so shipped documentation keeps the stronger `specify -> plan` mainline and the compatibility-only clarify positioning.

Purpose: Make the milestone truthful in the docs users are likely to follow first.
Output: Updated quickstart guidance plus focused doc regression coverage.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-surface-alignment-and-regression-hardening/09-CONTEXT.md
@.planning/phases/09-surface-alignment-and-regression-hardening/09-RESEARCH.md
@.planning/phases/09-surface-alignment-and-regression-hardening/09-01-SUMMARY.md
@README.md
@AGENTS.md
@docs/quickstart.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing assertions for workflow-guidance truthfulness</name>
  <files>tests/test_specify_guidance_docs.py</files>
  <read_first>README.md, AGENTS.md, docs/quickstart.md</read_first>
  <action>
    Create a focused regression test file that asserts the main user-facing guidance teaches `specify -> plan`, keeps `spec-extend` optional, and treats `clarify` as compatibility-only rather than a required mainline step. Make the test fail against the current quickstart guidance before the docs are updated.
  </action>
  <verify>pytest tests/test_specify_guidance_docs.py -q</verify>
  <acceptance_criteria>
    - `tests/test_specify_guidance_docs.py` exists with workflow-guidance assertions.
    - `pytest tests/test_specify_guidance_docs.py -q` fails before the docs are updated.
  </acceptance_criteria>
  <done>The doc regression suite now demands truthful workflow guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Update release-facing workflow guidance</name>
  <files>docs/quickstart.md</files>
  <read_first>docs/quickstart.md, README.md, AGENTS.md, .planning/phases/09-surface-alignment-and-regression-hardening/09-CONTEXT.md</read_first>
  <action>
    Update the quickstart and example flow so they teach `specify -> plan` as the mainline, treat `clarify` as compatibility-only, and mention `spec-extend` as the optional enhancement path. Keep the examples practical and concise.
  </action>
  <verify>rg -n "specify -> plan|spec-extend|compatibility|clarify" docs/quickstart.md</verify>
  <acceptance_criteria>
    - `docs/quickstart.md` no longer teaches `/speckit.clarify` as the default Step 4 after `/speckit.specify`.
    - `docs/quickstart.md` presents `specify -> plan` as the mainline.
    - `docs/quickstart.md` preserves compatibility-only clarify guidance if it still mentions clarify.
  </acceptance_criteria>
  <done>The release-facing quickstart now teaches the same workflow as the repo instructions.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the workflow-guidance regression suite</name>
  <files>tests/test_specify_guidance_docs.py</files>
  <read_first>tests/test_specify_guidance_docs.py, docs/quickstart.md</read_first>
  <action>
    Run the focused guidance regression test and make only the minimal follow-up adjustments needed so the guidance contract passes cleanly.
  </action>
  <verify>pytest tests/test_specify_guidance_docs.py -q</verify>
  <acceptance_criteria>
    - `pytest tests/test_specify_guidance_docs.py -q` exits with code 0.
    - The guidance assertions remain in the test file.
  </acceptance_criteria>
  <done>The user-facing workflow guidance is protected by regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_specify_guidance_docs.py -q` passes.
- [ ] `docs/quickstart.md` teaches `specify -> plan` as the mainline.
- [ ] Clarify is only described as compatibility or optional migration guidance.
</verification>

<success_criteria>
- All tasks completed
- User-facing guidance aligns with the stronger `sp-specify` behavior
- Regression tests protect workflow-guidance truthfulness
- SYNC-03 and the doc-facing part of SYNC-02 are covered
</success_criteria>

<output>
After completion, create `.planning/phases/09-surface-alignment-and-regression-hardening/09-02-SUMMARY.md`
</output>
