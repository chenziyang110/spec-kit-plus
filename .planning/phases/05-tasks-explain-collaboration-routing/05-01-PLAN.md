---
phase: 05-tasks-explain-collaboration-routing
plan: 01
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - templates/commands/tasks.md
  - templates/commands/explain.md
  - tests/test_alignment_templates.py
  - tests/test_tui_visual_contract.py
  - tests/test_extension_skills.py
  - tests/orchestration/test_policy.py
autonomous: true
requirements: [WF-03, WF-04, LANE-02, LANE-03]
must_haves:
  truths:
    - "`tasks` documents shared strategy selection before task decomposition begins."
    - "`tasks` defines workflow-specific lanes and join points aligned with dependency and write-set analysis."
    - "`explain` defaults to conservative single-agent behavior and only escalates when cross-check work is justified."
    - "Explain output still converges through one final render step after any supporting cross-check lanes."
  artifacts:
    - path: "templates/commands/tasks.md"
      provides: "Task-generation routing and lane-planning contract"
    - path: "templates/commands/explain.md"
      provides: "Conservative explanation routing and cross-check contract"
    - path: "tests/test_alignment_templates.py"
      provides: "Shared template contract coverage for `tasks` and `explain`"
    - path: "tests/test_tui_visual_contract.py"
      provides: "Explain TUI contract coverage"
    - path: "tests/test_extension_skills.py"
      provides: "Generated skill contract coverage for explain surfaces"
  key_links:
    - from: "templates/commands/tasks.md"
      to: "src/specify_cli/orchestration/policy.py"
      via: "shared strategy names and reason ordering"
    - from: "templates/commands/explain.md"
      to: "docs/superpowers/specs/2026-04-13-multi-cli-agent-collaboration-design.md"
      via: "conservative explain lanes and join-point behavior"
---

<objective>
Extend the shared orchestration contract into `tasks` and `explain` so task generation chooses a collaboration strategy before decomposition work begins and explanation stays conservative unless supporting cross-check fan-out is justified.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tasks-explain-collaboration-routing/05-CONTEXT.md
@docs/superpowers/specs/2026-04-13-multi-cli-agent-collaboration-design.md
@src/specify_cli/orchestration/policy.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add strategy-selection and lane-planning language to `tasks`</name>
  <files>templates/commands/tasks.md</files>
  <action>
    - Add an explicit strategy-selection step before task generation begins.
    - Use the canonical strategy names and fallback order from the shared policy.
    - Describe `tasks` lanes as story/phase decomposition, dependency analysis, and write-set / parallel-safety analysis.
    - Require join points before writing `tasks.md` and before emitting canonical parallel batches and join points.
  </action>
  <done>`tasks` documents the shared routing contract in a workflow-specific way.</done>
</task>

<task type="auto">
  <name>Task 2: Add conservative routing language to `explain`</name>
  <files>templates/commands/explain.md</files>
  <action>
    - Add explicit routing guidance that defaults `explain` to `single-agent`.
    - Describe the only justified fan-out lanes as primary artifact reading and supporting artifact cross-check.
    - Require a join point before the final explanation is rendered.
    - Keep the explanation grounded in the actual artifact set on disk.
  </action>
  <done>`explain` documents a conservative collaboration contract that matches the design.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression coverage for `tasks` and `explain` routing language</name>
  <files>tests/test_alignment_templates.py, tests/test_tui_visual_contract.py, tests/test_extension_skills.py, tests/orchestration/test_policy.py</files>
  <action>
    - Add shared template assertions for the new `tasks` and `explain` routing contract.
    - Extend explain TUI/skill tests so they assert the conservative routing language and cross-check join point.
    - Add generic policy tests proving the chooser remains command-agnostic for `tasks` and `explain`.
  </action>
  <verify>
    <automated>pytest tests/test_alignment_templates.py tests/test_tui_visual_contract.py tests/test_extension_skills.py tests/orchestration/test_policy.py -q</automated>
  </verify>
  <done>Regression coverage locks Phase 5 behavior in place.</done>
</task>

</tasks>

<success_criteria>
1. `tasks` selects and records a shared execution strategy before decomposition begins.
2. `tasks` documents workflow-specific lanes and join points for dependency and write-set analysis.
3. `explain` remains single-agent by default and only escalates for supporting cross-check work when justified.
4. Shared template and generated-skill tests confirm the new contract.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tasks-explain-collaboration-routing/05-01-SUMMARY.md` and `.planning/phases/05-tasks-explain-collaboration-routing/05-VERIFICATION.md`.
</output>
