---
phase: 02-contextual-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/specify_cli/debug/graph.py]
autonomous: true
requirements: [SYS-01, SYS-02, FND-04]

must_haves:
  truths:
    - "GatheringNode triggers context loading upon entry"
    - "GatheringNode enforces reproduction script verification before moving to investigation"
    - "Reproduction script must be confirmed to fail for the state to transition"
  artifacts:
    - path: "src/specify_cli/debug/graph.py"
      provides: "Updated GatheringNode with gate logic"
  key_links:
    - from: "src/specify_cli/debug/graph.py"
      to: "src/specify_cli/debug/context.py"
      via: "Method call in GatheringNode.run"
      pattern: "ContextLoader.*load_feature_context"
---

<objective>
Update the debug graph nodes to implement symptom collection and the "Reproduction First" gate.

Purpose: Ensure the agent has gathered sufficient proof of the bug before starting the investigation, avoiding "ghost" bug hunts.
Output: Enhanced GatheringNode and transition logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/specify_cli/debug/graph.py
@src/specify_cli/debug/schema.py
@src/specify_cli/debug/context.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GatheringNode to trigger context loading and symptom capture</name>
  <files>src/specify_cli/debug/graph.py</files>
  <action>
    Modify `GatheringNode.run`:
    1. Instantiate `ContextLoader` and find the active feature context.
    2. Populate `ctx.state.context` and `ctx.state.recently_modified` if not already set.
    3. Add logic to ensure `ctx.state.symptoms.expected` and `ctx.state.symptoms.actual` are populated.
    4. Since this is an autonomous node running in a graph, it should return its own class (loop) if prerequisites aren't met, or wait for agent tool calls to fill these in.
    Note: The actual tool-calling to fill these is an agent responsibility, but the node `run` method should check state completeness.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py</automated>
  </verify>
  <done>GatheringNode correctly triggers context loading and verifies presence of symptoms.</done>
</task>

<task type="auto">
  <name>Task 2: Implement "Reproduction First" gate in GatheringNode</name>
  <files>src/specify_cli/debug/graph.py</files>
  <action>
    Implement the gate logic in `GatheringNode.run`:
    1. Check `ctx.state.symptoms.reproduction` (the path to the script/test).
    2. Check `ctx.state.symptoms.reproduction_verified` (boolean, added in Plan 01).
    3. Transition to `InvestigatingNode` ONLY if `reproduction_verified` is True.
    4. If not verified, the node should return its own class (GatheringNode) or a new `AwaitingReproductionNode`. For now, return `self` to loop until verified.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py</automated>
  </verify>
  <done>Transition to investigation is blocked until a reproduction is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Agent → Graph State | Agent modifies state to satisfy gates |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | `GatheringNode` | mitigate | Ensure no infinite loops without progress; provide a way for human intervention. |
</threat_model>

<verification>
Unit test where `GatheringNode.run` is called with and without verified reproduction, checking the return type.
</verification>

<success_criteria>
- GatheringNode loads feature context automatically.
- Investigation cannot start without a verified reproduction script.
</success_criteria>

<output>
After completion, create `.planning/phases/02-contextual-intelligence/02-02-SUMMARY.md`
</output>
