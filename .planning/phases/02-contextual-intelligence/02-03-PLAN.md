---
phase: 02-contextual-intelligence
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [src/specify_cli/debug/graph.py]
autonomous: true
requirements: [SYS-02, CTX-02]

must_haves:
  truths:
    - "InvestigatingNode prioritizes 'Recently Modified Files' when starting a new hypothesis"
    - "Hypotheses that fail are recorded in the 'eliminated' list with evidence"
    - "The agent can query the current context through the state"
  artifacts:
    - path: "src/specify_cli/debug/graph.py"
      provides: "Enhanced InvestigatingNode with prioritization and elimination"
  key_links:
    - from: "src/specify_cli/debug/graph.py"
      to: "src/specify_cli/debug/schema.py"
      via: "State manipulation"
      pattern: "ctx.state.eliminated.append"
---

<objective>
Update the InvestigatingNode to utilize loaded context for better hypothesis generation and ensure thorough recording of eliminated theories.

Purpose: Speed up root cause discovery by prioritizing recently changed code and preventing circular reasoning.
Output: Updated InvestigatingNode.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/specify_cli/debug/graph.py
@src/specify_cli/debug/schema.py
@src/specify_cli/debug/context.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update InvestigatingNode to prioritize recently modified files</name>
  <files>src/specify_cli/debug/graph.py</files>
  <action>
    Modify `InvestigatingNode.run`:
    1. Update `ctx.state.current_focus.next_action` to include a summary of context (e.g., "Prioritizing files from recent git history: [list]").
    2. Ensure that any newly created hypotheses (when the agent is deciding where to look) are nudged towards `ctx.state.context.modified_files` and `ctx.state.recently_modified`.
    This serves as a hint to the LLM agent driving the node.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py</automated>
  </verify>
  <done>InvestigatingNode correctly prioritizes modified files in the focus summary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Hypothesis Elimination logic</name>
  <files>src/specify_cli/debug/graph.py</files>
  <action>
    Implement logic to handle hypothesis completion/failure in `InvestigatingNode.run`:
    1. If a hypothesis test is completed but fails to find the root cause, provide a mechanism to move the `current_focus` to the `eliminated` list.
    2. This ensures that the agent doesn't repeat the same tests.
    3. The node should stay in `InvestigatingNode` as long as there are more hypotheses to test or until root cause is found.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py</automated>
  </verify>
  <done>Hypothesis elimination is recorded in the state's audit trail.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for investigation prioritization and elimination</name>
  <files>tests/test_debug_graph_nodes.py</files>
  <action>
    Expand `tests/test_debug_graph_nodes.py` to cover:
    1. `GatheringNode` with reproduction gate logic (passing/failing).
    2. `InvestigatingNode` prioritization logic (checking `next_action` content).
    3. Elimination logic (checking if hypothesis moves to `eliminated` list).
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py</automated>
  </verify>
  <done>Comprehensive unit tests for graph logic and state transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Agent → State | Agent manages the hypothesis lifecycle |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | `InvestigatingNode` | mitigate | Ensure search space doesn't leak secrets if any exist in filenames (highly unlikely). |
</threat_model>

<verification>
Verify that failing a hypothesis correctly appends to the `eliminated` list.
Check that the state accurately reflects the current prioritized search space.
</verification>

<success_criteria>
- Investigation cycle is audit-trailed with eliminated theories.
- Search space is prioritized by recent changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-contextual-intelligence/02-03-SUMMARY.md`
</output>
