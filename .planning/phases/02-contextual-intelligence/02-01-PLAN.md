---
phase: 02-contextual-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/specify_cli/debug/schema.py, src/specify_cli/debug/context.py, tests/test_debug_graph_nodes.py]
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03]

must_haves:
  truths:
    - "Debug state stores feature context, git changes, and reproduction verification status"
    - "System identifies the active feature phase by parsing ROADMAP.md"
    - "System loads constitution.md and ROADMAP.md for system-level context"
    - "System parses plan.md files_modified to cross-reference with git changes"
  artifacts:
    - path: "src/specify_cli/debug/schema.py"
      provides: "Updated DebugGraphState with FeatureContext and reproduction_verified"
    - path: "src/specify_cli/debug/context.py"
      provides: "ContextLoader with artifact parsing and git analysis"
    - path: "tests/test_debug_graph_nodes.py"
      provides: "Initial test scaffold for graph nodes"
  key_links:
    - from: "src/specify_cli/debug/context.py"
      to: "ROADMAP.md"
      via: "File parsing"
      pattern: "ROADMAP\\.md"
---

<objective>
Implement the infrastructure for contextual intelligence: schema updates, artifact loading (including constitution and roadmap), and git history analysis with plan.md cross-referencing.

Purpose: Provide the agent with full system context (specs, roadmap, constitution) and prioritized search space (recent git changes vs plan files).
Output: Updated schema, context loading service, and test infrastructure.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/specify_cli/debug/schema.py
@src/specify_cli/debug/graph.py
@src/specify_cli/codex_team/auto_dispatch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DebugGraphState schema for context and verification</name>
  <files>src/specify_cli/debug/schema.py</files>
  <action>
    Add context-related fields to `src/specify_cli/debug/schema.py`:
    1. Define `FeatureContext` Pydantic model:
       - `feature_id`: str
       - `feature_name`: str
       - `feature_phase`: Optional[str] (derived from ROADMAP.md)
       - `spec_path`: Optional[str]
       - `plan_path`: Optional[str]
       - `tasks_path`: Optional[str]
       - `constitution_path`: Optional[str]
       - `roadmap_path`: Optional[str]
       - `modified_files`: List[str] (from plan.md)
    2. Add `context: FeatureContext = Field(default_factory=FeatureContext)` to `DebugGraphState`.
    3. Add `recently_modified: List[str] = Field(default_factory=list)` to `DebugGraphState`.
    4. Add `reproduction_verified: bool = False` to `Symptoms` model.
  </action>
  <verify>
    <automated>python -c "from specify_cli.debug.schema import DebugGraphState; s = DebugGraphState(slug='test', trigger='test'); assert hasattr(s, 'context'); assert hasattr(s.symptoms, 'reproduction_verified')"</automated>
  </verify>
  <done>Schema updated to hold system context and the reproduction gate state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ContextLoader with artifact and git analysis</name>
  <files>src/specify_cli/debug/context.py</files>
  <action>
    Implement `ContextLoader` in `src/specify_cli/debug/context.py`:
    1. `find_active_feature()`: Find feature dir in `specs/*` with newest `tasks.md`.
    2. `load_feature_context(feature_dir: Path) -> FeatureContext`:
       - Map standard paths (spec.md, plan.md, tasks.md).
       - Locate `constitution.md` and `ROADMAP.md`.
       - Parse `ROADMAP.md` to identify which phase the feature belongs to (CTX-03).
       - Parse the relevant `plan.md` to extract `files_modified` list.
    3. `get_recent_git_changes(limit: int = 10) -> List[str]`: Get list of files changed in last 10 commits.
    4. Implement cross-referencing: provide a way to identify which git-changed files overlap with the feature's `plan.md` (Decision: Cross-reference git changes with plan.md).
  </action>
  <verify>
    <automated>pytest tests/test_debug_context.py</automated>
  </verify>
  <done>ContextLoader can identify active features, parse their plans/roadmap, and analyze git history.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests for context loading and graph nodes</name>
  <files>tests/test_debug_context.py, tests/test_debug_graph_nodes.py</files>
  <action>
    1. Create/Update `tests/test_debug_context.py` to verify `ContextLoader` logic (feature discovery, ROADMAP parsing, plan parsing).
    2. Create `tests/test_debug_graph_nodes.py` as an initial scaffold with basic imports and a dummy test to satisfy Wave 1 verification for subsequent plans.
  </action>
  <verify>
    <automated>pytest tests/test_debug_context.py tests/test_debug_graph_nodes.py</automated>
  </verify>
  <done>Test infrastructure established for both context and future graph node updates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System → Agent | Agent reads Spec Kit artifacts (spec, plan, tasks, roadmap, constitution) |
| Git CLI → Agent | Agent reads git metadata |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `ContextLoader` | mitigate | Restrict file access to project root; validate paths. |
| T-02-02 | Tampering | `DebugGraphState` | mitigate | Use Pydantic for strict schema validation. |
</threat_model>

<verification>
Verify `DebugGraphState` serialization includes all new fields.
Verify `ContextLoader` correctly maps the active feature to a phase using `ROADMAP.md`.
</verification>

<success_criteria>
- Schema supports full context storage and reproduction gate.
- ContextLoader handles constitution, roadmap, and plan parsing.
- Git history can be cross-referenced with plan files.
- Test scaffold for graph nodes exists.
</success_criteria>

<output>
After completion, create `.planning/phases/02-contextual-intelligence/02-01-SUMMARY.md`
</output>
