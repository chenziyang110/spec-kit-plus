---
phase: 12-state-surfaces-and-end-to-end-verification
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - README.md
  - tests/contract/test_codex_team_cli_api_surface.py
  - tests/contract/test_codex_team_generated_assets.py
  - tests/contract/test_codex_team_auto_dispatch_cli.py
  - tests/integrations/test_cli.py
  - tests/integrations/test_integration_codex.py
autonomous: true
requirements: [STAT-02, STAT-03]
user_setup: []
must_haves:
  truths:
    - "Release-facing guidance explains `sp-implement` as a milestone-level orchestration leader with delegated execution and mixed failure handling."
    - "Cross-layer tests catch drift between shared template, generated surfaces, and runtime/contract behavior."
    - "The milestone-level execution loop is verified end-to-end against shipped expectations."
  artifacts:
    - path: "README.md"
      provides: "Release-facing guidance for the current runtime."
    - path: "tests/contract/test_codex_team_auto_dispatch_cli.py"
      provides: "Contract-level verification of dispatched batch and join-point behavior."
    - path: "tests/integrations/test_cli.py"
      provides: "Generated CLI/integration surface verification."
  key_links:
    - from: "README.md"
      to: "templates/commands/implement.md"
      via: "Release-facing docs must describe the same runtime semantics as the shared template."
    - from: "tests/contract/test_codex_team_auto_dispatch_cli.py"
      to: "tests/integrations/test_integration_codex.py"
      via: "E2E and generated-surface tests must fail together if runtime truth drifts."
---

<objective>
Add end-to-end verification and release-facing guidance for the leader/worker runtime in Phase 12.

Purpose: Make the shipped product story and the regression suite reflect the actual runtime the milestone now provides.
Output: Updated release-facing docs plus contract/integration verification that catches cross-layer drift.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing cross-layer regression and contract coverage for Phase 12 surfaces</name>
  <files>tests/contract/test_codex_team_cli_api_surface.py, tests/contract/test_codex_team_generated_assets.py, tests/contract/test_codex_team_auto_dispatch_cli.py, tests/integrations/test_cli.py, tests/integrations/test_integration_codex.py</files>
  <read_first>tests/contract/test_codex_team_cli_api_surface.py, tests/contract/test_codex_team_generated_assets.py, tests/contract/test_codex_team_auto_dispatch_cli.py, tests/integrations/test_cli.py, tests/integrations/test_integration_codex.py, README.md</read_first>
  <action>
    Extend the existing contract and integration suites so they assert the surfaced runtime truth end-to-end: generated Codex assets, CLI/runtime status payloads, auto-dispatch contract behavior, and generated `sp-implement` surfaces must all agree on leader orchestration, join points, delegated execution, and blocker/retry semantics. Keep the checks phrase- and field-based instead of snapshots.
  </action>
  <verify>pytest tests/contract/test_codex_team_cli_api_surface.py tests/contract/test_codex_team_generated_assets.py tests/contract/test_codex_team_auto_dispatch_cli.py tests/integrations/test_cli.py tests/integrations/test_integration_codex.py -q</verify>
  <acceptance_criteria>
    - at least one contract or integration test asserts surfaced blocker/retry/join-point truth
    - generated-asset and integration tests fail when generated `sp-implement` output drifts from runtime truth
    - `pytest tests/contract/test_codex_team_cli_api_surface.py tests/contract/test_codex_team_generated_assets.py tests/contract/test_codex_team_auto_dispatch_cli.py tests/integrations/test_cli.py tests/integrations/test_integration_codex.py -q` exits with code 0 after implementation
  </acceptance_criteria>
  <done>Phase 12 E2E and cross-layer drift checks are in place before docs are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update release-facing guidance for the leader/worker runtime</name>
  <files>README.md</files>
  <read_first>README.md, templates/commands/implement.md, .planning/phases/12-state-surfaces-and-end-to-end-verification/12-RESEARCH.md, .planning/phases/11-worker-dispatch-and-failure-convergence/11-VERIFICATION.md</read_first>
  <action>
    Update `README.md` so release-facing runtime guidance explains `/sp.implement` as a milestone-level orchestration leader that delegates execution, respects join points, surfaces blocker/retry state truthfully, and cooperates with `specify team` as the Codex runtime surface. Keep the wording aligned with the shared template and do not over-promise a richer UI or orchestration product than the repo actually ships.
  </action>
  <verify>rg -n \"sp-implement|leader|delegated|join point|blocker|retry|specify team\" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains release-facing language for delegated milestone orchestration
    - `README.md` references join points and blocker/retry truth in plain terms
    - `README.md` keeps `specify team` as the Codex runtime surface rather than inventing a new product boundary
  </acceptance_criteria>
  <done>Release-facing guidance now matches the runtime the milestone actually ships.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the shipped leader/worker runtime end-to-end</name>
  <files>tests/contract/test_codex_team_cli_api_surface.py, tests/contract/test_codex_team_generated_assets.py, tests/contract/test_codex_team_auto_dispatch_cli.py, tests/integrations/test_cli.py, tests/integrations/test_integration_codex.py, README.md</files>
  <read_first>tests/contract/test_codex_team_cli_api_surface.py, tests/contract/test_codex_team_generated_assets.py, tests/contract/test_codex_team_auto_dispatch_cli.py, tests/integrations/test_cli.py, tests/integrations/test_integration_codex.py, README.md</read_first>
  <action>
    Run and stabilize the combined contract and integration verification slice for Phase 12 so it proves the milestone-level execution loop coherently: generated runtime assets exist, generated `sp-implement` surfaces carry the surfaced runtime semantics, Codex runtime CLI/API contracts still hold, and release-facing guidance describes the same behavior. If assertions need refinement for truthful end-to-end coverage, update the focused test files above with exact fields and phrases that link docs, generated assets, and runtime behavior together.
  </action>
  <verify>pytest tests/contract/test_codex_team_cli_api_surface.py tests/contract/test_codex_team_generated_assets.py tests/contract/test_codex_team_auto_dispatch_cli.py tests/integrations/test_cli.py tests/integrations/test_integration_codex.py -q</verify>
  <acceptance_criteria>
    - the combined Phase 12 contract/integration suite passes
    - the tests assert a coherent leader/worker runtime story across docs, generated surfaces, and runtime contracts
    - no new release-facing drift remains between README guidance and generated `sp-implement` behavior
  </acceptance_criteria>
  <done>The shipped leader/worker runtime is now verified end-to-end against the milestone’s expectations.</done>
</task>

</tasks>
