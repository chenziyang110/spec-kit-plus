---
phase: 12-state-surfaces-and-end-to-end-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/specify_cli/__init__.py
  - src/specify_cli/codex_team/runtime_bridge.py
  - src/specify_cli/codex_team/batch_ops.py
  - .planning/STATE.md
  - .planning/PROJECT.md
  - templates/commands/implement.md
  - .agents/skills/sp-implement/SKILL.md
  - src/specify_cli/integrations/codex/__init__.py
  - tests/test_alignment_templates.py
  - tests/integrations/test_integration_codex.py
autonomous: true
requirements: [STAT-01, STAT-02]
user_setup: []
must_haves:
  truths:
    - "Planning and project artifacts surface worker outcomes, join points, retry states, and blockers using the runtime truth that already exists."
    - "The shared implement template and generated Codex skill remain aligned with the current leader/worker runtime behavior."
    - "Regression tests fail if artifact/state surfaces and generated implement surfaces drift from runtime truth."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Human-readable planning-state surface for runtime progress and blockers."
    - path: "templates/commands/implement.md"
      provides: "Shared implement contract that shipped surfaces inherit."
    - path: "tests/integrations/test_integration_codex.py"
      provides: "Generated-surface regression coverage for Codex init output."
  key_links:
    - from: "src/specify_cli/codex_team/runtime_bridge.py"
      to: ".planning/STATE.md"
      via: "Runtime retry/blocker truth must be reflected in planning artifacts rather than hidden in raw state files only."
    - from: "templates/commands/implement.md"
      to: ".agents/skills/sp-implement/SKILL.md"
      via: "Generated surfaces must mirror the shared contract instead of inventing a different runtime story."
---

<objective>
Persist runtime state surfaces and align shipped implement surfaces for Phase 12.

Purpose: Make the runtime truth from Phases 10-11 visible in planning/project artifacts and keep shared/generated implement surfaces aligned with that truth.
Output: Updated planning/status surfaces, aligned implement wording, and regression tests that catch cross-layer drift.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression coverage for surfaced runtime truth and generated implement alignment</name>
  <files>tests/test_alignment_templates.py, tests/integrations/test_integration_codex.py</files>
  <read_first>tests/test_alignment_templates.py, tests/integrations/test_integration_codex.py, .planning/phases/12-state-surfaces-and-end-to-end-verification/12-CONTEXT.md, .planning/phases/12-state-surfaces-and-end-to-end-verification/12-RESEARCH.md</read_first>
  <action>
    Extend `tests/test_alignment_templates.py` so the shared `implement` template must mention join-point awareness, retry/blocker truth, or equivalent surfaced runtime semantics in addition to the existing leader-only contract. Extend `tests/integrations/test_integration_codex.py` so generated Codex `sp-implement` output must include the same surfaced runtime wording, not only the strategy vocabulary and leader contract.
  </action>
  <verify>pytest tests/test_alignment_templates.py tests/integrations/test_integration_codex.py -q</verify>
  <acceptance_criteria>
    - `tests/test_alignment_templates.py` asserts surfaced runtime concepts beyond the existing leader-only wording.
    - `tests/integrations/test_integration_codex.py` asserts generated `sp-implement` output includes the same runtime-state truth.
    - `pytest tests/test_alignment_templates.py tests/integrations/test_integration_codex.py -q` exits with code 0 after implementation.
  </acceptance_criteria>
  <done>Phase 12 surface-alignment work is locked behind regression coverage before docs/templates change.</done>
</task>

<task type="auto">
  <name>Task 2: Surface runtime truth into planning and project state artifacts</name>
  <files>src/specify_cli/__init__.py, src/specify_cli/codex_team/runtime_bridge.py, src/specify_cli/codex_team/batch_ops.py, .planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>src/specify_cli/__init__.py, src/specify_cli/codex_team/runtime_bridge.py, src/specify_cli/codex_team/batch_ops.py, .planning/STATE.md, .planning/PROJECT.md, .planning/phases/11-worker-dispatch-and-failure-convergence/11-VERIFICATION.md</read_first>
  <action>
    Update the relevant status/reporting helpers in `src/specify_cli/__init__.py` and any needed runtime summary entry points in `src/specify_cli/codex_team/runtime_bridge.py` or `src/specify_cli/codex_team/batch_ops.py` so surfaced state can report worker outcomes, open join points, retry-pending work, and blockers in a stable human-readable shape. Evolve `.planning/STATE.md` and `.planning/PROJECT.md` templates/content so they explicitly reflect that runtime truth rather than only high-level phase completion language.
  </action>
  <verify>rg -n \"join point|retry_pending|blocker|worker outcomes|blocked\" src/specify_cli/__init__.py src/specify_cli/codex_team/runtime_bridge.py src/specify_cli/codex_team/batch_ops.py .planning/STATE.md .planning/PROJECT.md</verify>
  <acceptance_criteria>
    - surfaced status/reporting code references join points, retry-pending work, or blockers explicitly
    - `.planning/STATE.md` contains runtime-truth language rather than phase-level abstraction only
    - `.planning/PROJECT.md` reflects the shipped runtime truth from Phase 11
  </acceptance_criteria>
  <done>Planning/project artifacts now expose the runtime truth that was previously buried in Codex-team state files.</done>
</task>

<task type="auto">
  <name>Task 3: Align shared and generated implement surfaces with the surfaced runtime truth</name>
  <files>templates/commands/implement.md, .agents/skills/sp-implement/SKILL.md, src/specify_cli/integrations/codex/__init__.py</files>
  <read_first>templates/commands/implement.md, .agents/skills/sp-implement/SKILL.md, src/specify_cli/integrations/codex/__init__.py, .planning/phases/12-state-surfaces-and-end-to-end-verification/12-CONTEXT.md</read_first>
  <action>
    Update `templates/commands/implement.md` so the shared workflow text reflects the shipped runtime truth: delegated execution uses explicit join points, can surface retry-pending/blocker states, and reports blockers truthfully rather than implying hidden orchestration. Update `.agents/skills/sp-implement/SKILL.md` and `src/specify_cli/integrations/codex/__init__.py` so the checked-in and generated Codex surfaces mirror the same wording without diverging from the shared template.
  </action>
  <verify>pytest tests/test_alignment_templates.py tests/integrations/test_integration_codex.py -q</verify>
  <acceptance_criteria>
    - `templates/commands/implement.md` mentions join points and retry/blocker truth in the milestone runtime description
    - `.agents/skills/sp-implement/SKILL.md` mirrors that same surfaced runtime wording
    - `src/specify_cli/integrations/codex/__init__.py` injects matching generated-skill wording
    - `pytest tests/test_alignment_templates.py tests/integrations/test_integration_codex.py -q` exits with code 0
  </acceptance_criteria>
  <done>Shared and generated implement surfaces now describe the same surfaced runtime truth.</done>
</task>

</tasks>
