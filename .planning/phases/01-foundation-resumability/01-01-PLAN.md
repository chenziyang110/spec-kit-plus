---
phase: 01-foundation-resumability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/specify_cli/debug/__init__.py, src/specify_cli/debug/graph.py, src/specify_cli/debug/schema.py, tests/test_debug_graph.py]
autonomous: true
requirements: [FND-01]

must_haves:
  truths:
    - "The debugging engine correctly transitions through the investigation lifecycle (Gather -> Investigate -> Fix -> Verify) based on input."
    - "Users can observe the engine capturing hypotheses, evidence, and eliminated theories during an active session."
  artifacts:
    - path: "src/specify_cli/debug/schema.py"
      provides: "Graph state and Node base classes"
    - path: "src/specify_cli/debug/graph.py"
      provides: "Pydantic-graph definition and node implementations"
  key_links:
    - from: "src/specify_cli/debug/graph.py"
      to: "pydantic_graph.Graph"
      via: "inheritance and registration"
---

<objective>
Establish the core pydantic-graph structure for the sp-debug feature. This includes defining the shared state, node interfaces, and the basic investigation loop transitions.

Purpose: Provide a type-safe, FSM-based engine for systematic debugging.
Output: Working graph engine with skeleton nodes for the investigation lifecycle.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-resumability/01-CONTEXT.md
@.planning/research/SUMMARY.md

<interfaces>
<!-- Expected pydantic-graph patterns based on 2025 standards -->
```python
from pydantic import BaseModel
from pydantic_graph import BaseNode, Graph, GraphRunContext, End

class DebugState(BaseModel):
    trigger: str
    status: str
    current_node_id: str | None = None
    hypothesis: str | None = None
    evidence: list[dict] = []
    eliminated: list[dict] = []
    # ... other fields from templates/debug.md

class DebugNode(BaseNode[DebugState]):
    async def run(self, ctx: GraphRunContext[DebugState]) -> 'DebugNode | End':
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Dependencies & Structure</name>
  <files>pyproject.toml, src/specify_cli/debug/__init__.py</files>
  <action>
    - Add `pydantic-ai` and `pydantic-graph` to the `dependencies` list in `pyproject.toml`.
    - Create the `src/specify_cli/debug/` directory and an empty `__init__.py`.
  </action>
  <verify>
    <automated>grep -E "pydantic-(ai|graph)" pyproject.toml</automated>
  </verify>
  <done>Dependencies added and package directory created.</done>
</task>

<task type="auto">
  <name>Task 2: Define Graph State and Nodes</name>
  <files>src/specify_cli/debug/schema.py, src/specify_cli/debug/graph.py</files>
  <action>
    - In `schema.py`, define `DebugGraphState` Pydantic model with fields mapping to `templates/debug.md` frontmatter and sections.
    - MUST include `current_node_id` in the schema per D-02.
    - In `graph.py`, define the 6 core nodes as `pydantic_graph.BaseNode` subclasses:
      1. `GatheringNode`
      2. `InvestigatingNode`
      3. `FixingNode`
      4. `VerifyingNode`
      5. `AwaitingHumanNode`
      6. `ResolvedNode` (Terminal state)
    - Register these nodes in a `pydantic_graph.Graph` instance.
  </action>
  <verify>
    <automated>python -c "from specify_cli.debug.graph import debug_graph; print(debug_graph)"</automated>
  </verify>
  <done>Graph state and nodes defined and registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Skeleton Transition Logic</name>
  <files>src/specify_cli/debug/graph.py, tests/test_debug_graph.py</files>
  <behavior>
    - Test 1: GatheringNode transitions to InvestigatingNode when symptoms are captured.
    - Test 2: InvestigatingNode transitions to FixingNode on confirmed root cause.
    - Test 3: FixingNode transitions to VerifyingNode after fix application.
    - Test 4: VerifyingNode transitions to ResolvedNode on success or InvestigatingNode on failure.
  </behavior>
  <action>
    Implement minimal `run()` methods for each node to support the state machine flow described in `SKILL.md`. Use placeholder logic for now as Phase 1 focuses on the structure.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph.py</automated>
  </verify>
  <done>Basic transition logic implemented and verified by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Graph State | Internal state management for the debugging loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | D | Graph Runner | mitigate | Implement a `max_steps` or loop detection in the graph execution to prevent infinite loops in the investigation phase. |
</threat_model>

<verification>
Run `pytest tests/test_debug_graph.py` to ensure all state transitions function as expected.
</verification>

<success_criteria>
- Pydantic-graph nodes for all 6 investigation stages are defined.
- Unit tests prove the graph can transition from Gathering through to Resolved.
- New dependencies are correctly registered in pyproject.toml.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-resumability/01-01-SUMMARY.md`
</output>
