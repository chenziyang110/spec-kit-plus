---
phase: 01-foundation-resumability
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [src/specify_cli/debug/cli.py, src/specify_cli/__init__.py, tests/test_debug_cli.py]
autonomous: false
requirements: [FND-03, TOL-03]

must_haves:
  truths:
    - "User can call `specify debug` without arguments to resume the most recent session."
    - "User can call `specify debug 'bug message'` to start a new session."
    - "The CLI correctly re-inflates the graph state from the persisted file."
  artifacts:
    - path: "src/specify_cli/debug/cli.py"
      provides: "Typer debug command implementation"
  key_links:
    - from: "src/specify_cli/__init__.py"
      to: "src/specify_cli/debug/cli.py"
      via: "typer app registration"
    - from: "src/specify_cli/debug/cli.py"
      to: "src/specify_cli/debug/graph.py"
      via: "graph runner instantiation"
---

<objective>
Expose the sp-debug feature through the CLI and implement the auto-resume logic to ensure a seamless investigator experience across sessions.

Purpose: Provide the primary user entry point and ensure investigation continuity.
Output: Working `specify debug` command with automatic session recovery.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/specify_cli/__init__.py

<interfaces>
From src/specify_cli/debug/persistence.py (Plan 02):
```python
def load_most_recent_session() -> DebugGraphState | None:
    ...
```

From src/specify_cli/debug/graph.py (Plan 01):
```python
async def run_debug_session(state: DebugGraphState):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Debug CLI Command</name>
  <files>src/specify_cli/debug/cli.py</files>
  <action>
    - Create a Typer application (or just a command function) for `debug`.
    - Define arguments: `description: Optional[str] = None`.
    - Implement the logic:
      - If `description` is provided: `generate_slug()`, create `DebugGraphState`, start new session.
      - If `description` is NOT provided: Call `load_most_recent_session()`. If found, resume. If not found, show error/help.
  </action>
  <verify>
    <automated>python -c "from specify_cli.debug.cli import debug_command; print(debug_command)"</automated>
  </verify>
  <done>Debug CLI command implemented with session branching logic.</done>
</task>

<task type="auto">
  <name>Task 2: Register in specify CLI with Mandatory Alias</name>
  <files>src/specify_cli/__init__.py</files>
  <action>
    - Import the `debug` command from `src.specify_cli.debug.cli`.
    - Register it with the main `app` using `app.command(name="debug")(debug_command)`.
    - MANDATORY: Add an alias `sp-debug` for the command per TOL-03 requirement in ROADMAP. Ensure both `specify debug` and `specify sp-debug` (or direct `sp-debug` if configured as entry point) are available.
  </action>
  <verify>
    <automated>specify --help | grep debug && specify --help | grep sp-debug</automated>
  </verify>
  <done>The `debug` command and `sp-debug` alias are visible and invokable from the CLI.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify End-to-End Resume Flow</name>
  <files>.planning/debug/</files>
  <action>
    Manual verification of the resumability promise:
    1. Run `specify debug "Test bug"` -> Session starts.
    2. Wait for it to initialize (Gathering state) then Ctrl+C.
    3. Verify a file exists in `.planning/debug/`.
    4. Run `specify debug` without arguments.
    5. Verify it announces "Resuming session [slug]" and restores the state.
    6. Verify `sp-debug` works as an alias.
  </action>
  <verify>
    User confirms "approved" after performing the steps.
  </verify>
  <done>Resumability and aliasing are confirmed working as expected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Arguments | Untrusted user input via command line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | S | CLI Command | mitigate | Sanitize `description` argument before using it in filenames or printing to console to prevent path traversal or terminal injection. |
</threat_model>

<verification>
Run `specify debug "Test"` and check if file is created. Kill it, then run `specify debug` and see if it resumes.
</verification>

<success_criteria>
- `specify debug` command is fully operational.
- `sp-debug` alias is registered and functional per TOL-03.
- Auto-resume logic correctly identifies and reloads the most recent unfinished session.
- New sessions are created when a description is provided.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-resumability/01-03-SUMMARY.md`
</output>
