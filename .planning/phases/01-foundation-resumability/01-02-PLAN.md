---
phase: 01-foundation-resumability
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/specify_cli/debug/persistence.py, src/specify_cli/debug/utils.py, tests/test_debug_persistence.py]
autonomous: true
requirements: [FND-02]

must_haves:
  truths:
    - "Investigation state is persisted to a human-readable Markdown file in .planning/debug/[slug].md."
    - "Slug generation handles special characters and creates unique names with timestamps."
    - "Persistence happens before each transition to ensure resumes work correctly."
  artifacts:
    - path: "src/specify_cli/debug/persistence.py"
      provides: "Markdown and YAML serialization/deserialization logic"
    - path: "src/specify_cli/debug/utils.py"
      provides: "Slug generation and path helpers"
  key_links:
    - from: "src/specify_cli/debug/persistence.py"
      to: ".planning/debug/*.md"
      via: "file write and read operations"
---

<objective>
Implement the Markdown-based persistence handler that ensures debug sessions are human-readable, auditable, and resumable.

Purpose: Guarantee no loss of investigation state and enable human-in-the-loop review.
Output: Persistent session storage following the `templates/debug.md` format.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-resumability/01-CONTEXT.md
@templates/debug.md

<interfaces>
From src/specify_cli/debug/schema.py (Plan 01):
```python
class DebugGraphState(BaseModel):
    slug: str
    status: str
    trigger: str
    current_node_id: str | None
    # ... other fields
```

Key persistence requirements from `templates/debug.md`:
- YAML Frontmatter (status, trigger, current_node_id, created, updated)
- Markdown Sections (Current Focus, Symptoms, Eliminated, Evidence, Resolution)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Persistence Handler</name>
  <files>src/specify_cli/debug/persistence.py</files>
  <action>
    - Create `MarkdownPersistenceHandler` class.
    - Implement `save(state: DebugGraphState)`: Converts the state into the exact Markdown format defined in `templates/debug.md`.
    - MUST include `current_node_id` in the YAML frontmatter per D-02.
    - Implement `load(path: Path) -> DebugGraphState`: Re-inflates the state object from the Markdown file, parsing YAML and extracting section content.
    - Ensure it uses `PyYAML` for frontmatter (already in `pyproject.toml`).
  </action>
  <verify>
    <automated>python -c "from specify_cli.debug.persistence import MarkdownPersistenceHandler; print(MarkdownPersistenceHandler)"</automated>
  </verify>
  <done>Persistence handler correctly serializes and deserializes the debug state including the current node ID.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Slug and Utility Logic</name>
  <files>src/specify_cli/debug/utils.py</files>
  <action>
    - Implement `generate_slug(trigger: str) -> str`: Sanitizes trigger (e.g., "API Timeout -> api-timeout") and prepends a timestamp.
    - Implement `get_debug_dir() -> Path`: Returns the path to `.planning/debug/`, ensuring it exists.
  </action>
  <verify>
    <automated>python -c "from specify_cli.debug.utils import generate_slug; print(generate_slug('Some Bug Description'))"</automated>
  </verify>
  <done>Slug and path utilities are available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration Persistence into Graph</name>
  <files>src/specify_cli/debug/graph.py, tests/test_debug_persistence.py</files>
  <behavior>
    - Test 1: Saving a new state creates a file with correct YAML frontmatter (including current_node_id).
    - Test 2: Loading that same file recreates an identical state object.
    - Test 3: Updating status in state and saving overwrites the YAML frontmatter.
    - Test 4: Appending to Evidence or Eliminated lists reflects as bullet points in the Markdown.
  </behavior>
  <action>
    Update the node base class or graph runner to invoke `persistence.save()` before and after every node's `run()` method. This fulfills the "Update BEFORE taking action" rule from SKILL.md.
  </action>
  <verify>
    <automated>pytest tests/test_debug_persistence.py</automated>
  </verify>
  <done>Graph state is automatically persisted during the investigation loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/debug/ | File system storage for session state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | I | Persistence Handler | mitigate | Ensure the `.planning/` directory is excluded from git if sensitive context is expected, or warn user if it's tracked. Use safe YAML loading to prevent arbitrary code execution from modified debug files. |
</threat_model>

<verification>
Run `pytest tests/test_debug_persistence.py` to ensure state can be reliably saved and reloaded without data loss.
</verification>

<success_criteria>
- `MarkdownPersistenceHandler` fully implements the `templates/debug.md` specification.
- A debug session can be re-inflated from its Markdown file without state loss.
- Slugs are unique, URL-safe, and include creation timestamps.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-resumability/01-02-SUMMARY.md`
</output>
