---
phase: 10-leader-contract-and-milestone-scheduler
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - templates/commands/implement.md
  - .agents/skills/sp-implement/SKILL.md
  - src/specify_cli/integrations/codex/__init__.py
  - src/specify_cli/orchestration/scheduler.py
  - tests/orchestration/test_policy.py
  - tests/codex_team/test_implement_runtime_routing.py
  - tests/integrations/test_integration_codex.py
autonomous: true
requirements: [ORCH-01, LEAD-02]
user_setup: []
must_haves:
  truths:
    - "`single-agent` means a delegated single-worker lane rather than leader self-execution."
    - "`sp-implement` can continue from one finished phase into the next phase of the same milestone without requiring a manual restart."
    - "Codex-specific guidance stays aligned with the shared implement contract instead of inventing a different leader model."
  artifacts:
    - path: "templates/commands/implement.md"
      provides: "The canonical milestone-loop and delegated sequential execution contract."
    - path: ".agents/skills/sp-implement/SKILL.md"
      provides: "The shipped Codex mirror of the shared implement contract."
    - path: "tests/integrations/test_integration_codex.py"
      provides: "Regression coverage for generated Codex skill alignment."
  key_links:
    - from: "templates/commands/implement.md"
      to: ".agents/skills/sp-implement/SKILL.md"
      via: "The generated skill must inherit the same delegated sequential semantics."
    - from: "src/specify_cli/integrations/codex/__init__.py"
      to: "tests/integrations/test_integration_codex.py"
      via: "Codex post-processing must preserve, not replace, the shared leader contract."
---

<objective>
Integrate the Phase 10 scheduler contract into the shipped implement flow so the runtime can continue through the milestone and route sequential execution through delegated worker lanes.

Purpose: Turn the leader-only contract into an implement flow that can actually drive milestone progression in the shipped surfaces.
Output: Updated implement flow, aligned Codex guidance, and tests proving delegated sequential milestone continuation.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-leader-contract-and-milestone-scheduler/10-RESEARCH.md
@.planning/phases/10-leader-contract-and-milestone-scheduler/10-01-PLAN.md
@AGENTS.md
@templates/commands/implement.md
@.agents/skills/sp-implement/SKILL.md
@src/specify_cli/integrations/codex/__init__.py
@src/specify_cli/orchestration/scheduler.py
@tests/orchestration/test_policy.py
@tests/codex_team/test_implement_runtime_routing.py
@tests/integrations/test_integration_codex.py
</context>

<threat_model>
<trust_boundaries>
- Shared implement template -> generated Codex skill -> user-visible runtime contract
- Milestone scheduler helpers -> implement flow wording -> selected next phase and sequential execution path
- Strategy selection policy -> delegated sequential execution semantics
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>P10-02-T1</threat_id>
    <category>Tampering</category>
    <component>delegated sequential execution path</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Make `single-agent` explicitly mean delegated single-worker execution and add tests that fail if the leader is described as the direct executor.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>P10-02-T2</threat_id>
    <category>Repudiation</category>
    <component>Codex-specific implement guidance</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep Codex addenda aligned with the shared contract and test the generated skill so milestone continuation wording does not drift between surfaces.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update the implement flow for milestone continuation and delegated sequential execution</name>
  <files>templates/commands/implement.md, src/specify_cli/orchestration/scheduler.py</files>
  <read_first>templates/commands/implement.md, src/specify_cli/orchestration/scheduler.py, .planning/ROADMAP.md, .planning/phases/10-leader-contract-and-milestone-scheduler/10-RESEARCH.md</read_first>
  <action>
    Rewrite the implement execution outline so it states that after each completed batch the leader re-evaluates milestone state, selects the next executable phase, and continues automatically until the milestone is complete or blocked. In the same contract, define `single-agent` as a delegated single-worker lane. Update `src/specify_cli/orchestration/scheduler.py` if needed so the helper API exposes the values the template now names, including a continue-or-stop decision for the next phase.
  </action>
  <verify>rg -n "single-worker|next executable phase|milestone|blocked|continue" templates/commands/implement.md src/specify_cli/orchestration/scheduler.py</verify>
  <acceptance_criteria>
    - `templates/commands/implement.md` contains `single-worker` or equivalent delegated sequential wording.
    - `templates/commands/implement.md` describes continuing into the next executable phase of the milestone.
    - `src/specify_cli/orchestration/scheduler.py` contains a continue-or-stop decision field or helper usage matching the template wording.
  </acceptance_criteria>
  <done>The implement flow now covers milestone continuation and delegated sequential execution.</done>
</task>

<task type="auto">
  <name>Task 2: Align the shipped Codex skill and integration post-processing with the shared contract</name>
  <files>.agents/skills/sp-implement/SKILL.md, src/specify_cli/integrations/codex/__init__.py</files>
  <read_first>.agents/skills/sp-implement/SKILL.md, src/specify_cli/integrations/codex/__init__.py, templates/commands/implement.md</read_first>
  <action>
    Update the generated Codex implement skill and the Codex integration post-processing logic so the addendum preserves the shared leader-only contract and delegated `single-agent` worker semantics. Keep the `specify team` escalation path only as a Codex-specific interpretation of `sidecar-runtime`; do not let the addendum reintroduce leader self-execution or a Codex-only milestone loop different from the shared template.
  </action>
  <verify>rg -n "single-worker|leader|sidecar-runtime|specify team|milestone" .agents/skills/sp-implement/SKILL.md src/specify_cli/integrations/codex/__init__.py</verify>
  <acceptance_criteria>
    - `.agents/skills/sp-implement/SKILL.md` includes leader-oriented wording consistent with the shared template.
    - `.agents/skills/sp-implement/SKILL.md` defines `single-agent` as a delegated worker path or equivalent.
    - `src/specify_cli/integrations/codex/__init__.py` still treats `sidecar-runtime` as `specify team` without replacing the shared contract vocabulary.
  </acceptance_criteria>
  <done>The shipped Codex surface now mirrors the shared Phase 10 contract instead of drifting from it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression coverage for delegated sequential semantics and generated-skill alignment</name>
  <files>tests/orchestration/test_policy.py, tests/codex_team/test_implement_runtime_routing.py, tests/integrations/test_integration_codex.py</files>
  <read_first>tests/orchestration/test_policy.py, tests/codex_team/test_implement_runtime_routing.py, tests/integrations/test_integration_codex.py, templates/commands/implement.md, src/specify_cli/integrations/codex/__init__.py</read_first>
  <action>
    Extend the orchestration and integration regression suite so it proves the Phase 10 flow. Add assertions that the implement contract can continue through the milestone, that delegated sequential execution is described as a worker path, and that the generated Codex skill inherits the same semantics. Keep tests concrete and phrase-based instead of snapshot-based.
  </action>
  <verify>pytest tests/orchestration/test_policy.py tests/codex_team/test_implement_runtime_routing.py tests/integrations/test_integration_codex.py -q</verify>
  <acceptance_criteria>
    - `tests/codex_team/test_implement_runtime_routing.py` asserts delegated sequential or single-worker wording.
    - `tests/integrations/test_integration_codex.py` asserts generated `sp-implement` skill alignment with the shared contract.
    - `pytest tests/orchestration/test_policy.py tests/codex_team/test_implement_runtime_routing.py tests/integrations/test_integration_codex.py -q` exits with code 0.
  </acceptance_criteria>
  <done>The regression suite now protects milestone continuation and delegated sequential behavior from drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "single-worker|next executable phase|milestone|blocked" templates/commands/implement.md` returns matches.
- [ ] `rg -n "leader|single-worker|specify team|sidecar-runtime" .agents/skills/sp-implement/SKILL.md src/specify_cli/integrations/codex/__init__.py` returns matches.
- [ ] `pytest tests/orchestration/test_policy.py tests/codex_team/test_implement_runtime_routing.py tests/integrations/test_integration_codex.py -q` passes.
</verification>

<success_criteria>
- All tasks completed
- The shipped implement flow can continue through the milestone in contract terms
- Sequential execution is delegated through worker semantics rather than leader self-execution
- Codex-generated skill behavior remains aligned with the shared contract
</success_criteria>

<output>
After completion, create `.planning/phases/10-leader-contract-and-milestone-scheduler/10-02-SUMMARY.md`
</output>
