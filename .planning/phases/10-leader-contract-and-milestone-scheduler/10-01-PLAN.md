---
phase: 10-leader-contract-and-milestone-scheduler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/specify_cli/orchestration/models.py
  - src/specify_cli/orchestration/state_store.py
  - src/specify_cli/orchestration/__init__.py
  - src/specify_cli/orchestration/scheduler.py
  - templates/commands/implement.md
  - tests/test_alignment_templates.py
  - tests/codex_team/test_implement_runtime_routing.py
autonomous: true
requirements: [ORCH-02, LEAD-01]
user_setup: []
must_haves:
  truths:
    - "`sp-implement` is described as a leader-only runtime that schedules and dispatches work instead of performing concrete implementation tasks itself."
    - "The orchestration core exposes explicit scheduler primitives for selecting the next executable phase and ready batch."
    - "The shared implement template remains the primary source of truth for the leader-only contract."
  artifacts:
    - path: "templates/commands/implement.md"
      provides: "The shared implement contract all integrations inherit."
    - path: "src/specify_cli/orchestration/scheduler.py"
      provides: "The roadmap-aware scheduler helper for milestone execution."
    - path: "tests/codex_team/test_implement_runtime_routing.py"
      provides: "Regression coverage for leader-only runtime wording in the shipped implement surface."
  key_links:
    - from: "src/specify_cli/orchestration/scheduler.py"
      to: "templates/commands/implement.md"
      via: "The runtime contract should describe the same milestone-scheduling concepts implemented in code."
    - from: "templates/commands/implement.md"
      to: "tests/test_alignment_templates.py"
      via: "Template assertions must fail if leader-only wording or milestone scheduling language drifts."
---

<objective>
Define the shared leader-only contract and scheduler primitives for Phase 10 so `sp-implement` can reason about milestone-wide next steps without executing implementation work directly.

Purpose: Create the shared semantic and code foundation that later delegated execution plans can build on.
Output: New scheduler/runtime primitives plus shared implement-contract updates and focused regression coverage.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/10-leader-contract-and-milestone-scheduler/10-RESEARCH.md
@AGENTS.md
@templates/commands/implement.md
@src/specify_cli/orchestration/models.py
@src/specify_cli/orchestration/state_store.py
@src/specify_cli/orchestration/events.py
@tests/test_alignment_templates.py
@tests/codex_team/test_implement_runtime_routing.py
</context>

<threat_model>
<trust_boundaries>
- Shared implement contract -> generated skills and integration-facing runtime behavior
- Roadmap and task metadata -> scheduler helper -> selected next phase and ready batch
- Scheduler models -> persisted orchestration state files under `.specify/orchestration/`
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>P10-01-T1</threat_id>
    <category>Tampering</category>
    <component>templates/commands/implement.md</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Make the shared template state explicitly that the invoking runtime is the leader and that concrete implementation work happens in delegated worker lanes only.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>P10-01-T2</threat_id>
    <category>Repudiation</category>
    <component>milestone scheduler primitives</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Add explicit scheduler models and persistence helpers so milestone next-step selection is represented in code rather than implied only by prompt text.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add milestone scheduler models and persistence helpers</name>
  <files>src/specify_cli/orchestration/models.py, src/specify_cli/orchestration/state_store.py, src/specify_cli/orchestration/__init__.py</files>
  <read_first>src/specify_cli/orchestration/models.py, src/specify_cli/orchestration/state_store.py, src/specify_cli/orchestration/events.py, .planning/phases/10-leader-contract-and-milestone-scheduler/10-RESEARCH.md</read_first>
  <action>
    Extend `src/specify_cli/orchestration/models.py` with explicit scheduler-facing dataclasses for milestone execution state and next-ready work selection. Use concrete names such as `PhaseExecutionState` and `MilestoneExecutionDecision`, include fields for `phase_number`, `phase_name`, `ready_batch_count`, `leader_mode`, and `continue_milestone`. Extend `src/specify_cli/orchestration/state_store.py` with canonical path helpers for milestone-state records and decision records, then export the new helpers from `src/specify_cli/orchestration/__init__.py`.
  </action>
  <verify>rg -n "class PhaseExecutionState|class MilestoneExecutionDecision|def milestone_state_path|def decision_path" src/specify_cli/orchestration/models.py src/specify_cli/orchestration/state_store.py src/specify_cli/orchestration/__init__.py</verify>
  <acceptance_criteria>
    - `src/specify_cli/orchestration/models.py` defines `PhaseExecutionState`.
    - `src/specify_cli/orchestration/models.py` defines `MilestoneExecutionDecision`.
    - `src/specify_cli/orchestration/state_store.py` defines `milestone_state_path` and `decision_path`.
    - `src/specify_cli/orchestration/__init__.py` re-exports the new scheduler-facing symbols.
  </acceptance_criteria>
  <done>The orchestration core now has explicit data structures for milestone scheduling decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Create a roadmap-aware scheduler helper for next-ready work selection</name>
  <files>src/specify_cli/orchestration/scheduler.py</files>
  <read_first>src/specify_cli/orchestration/policy.py, src/specify_cli/orchestration/models.py, .planning/ROADMAP.md, .planning/phases/10-leader-contract-and-milestone-scheduler/10-RESEARCH.md</read_first>
  <action>
    Create `src/specify_cli/orchestration/scheduler.py` with concrete helpers that can select the next executable phase and construct a milestone decision for the ready batch. Implement functions named `select_next_phase` and `build_milestone_execution_decision`. The helper should preserve numeric roadmap order, accept current phase/task state as input, and return a structure that says which phase runs next and whether the milestone continues automatically after the current batch.
  </action>
  <verify>rg -n "def select_next_phase|def build_milestone_execution_decision|continue_milestone" src/specify_cli/orchestration/scheduler.py</verify>
  <acceptance_criteria>
    - `src/specify_cli/orchestration/scheduler.py` exists.
    - `src/specify_cli/orchestration/scheduler.py` defines `select_next_phase`.
    - `src/specify_cli/orchestration/scheduler.py` defines `build_milestone_execution_decision`.
    - `src/specify_cli/orchestration/scheduler.py` contains roadmap-order language or code comments indicating milestone continuation behavior.
  </acceptance_criteria>
  <done>The codebase now has a concrete scheduler module instead of only implicit milestone-loop language.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite the shared implement contract around leader-only scheduling</name>
  <files>templates/commands/implement.md, tests/test_alignment_templates.py, tests/codex_team/test_implement_runtime_routing.py</files>
  <read_first>templates/commands/implement.md, tests/test_alignment_templates.py, tests/codex_team/test_implement_runtime_routing.py, AGENTS.md</read_first>
  <action>
    Update `templates/commands/implement.md` so Step 6 and Step 7 explicitly describe the invoking runtime as a leader that reads planning artifacts, selects the next executable phase and batch, and dispatches work instead of performing the implementation directly. Preserve the exact strategy vocabulary `single-lane`, `native-multi-agent`, and `sidecar-runtime`. Add regression assertions in `tests/test_alignment_templates.py` and `tests/codex_team/test_implement_runtime_routing.py` for the leader-only contract, milestone-wide next-step selection, and shared-template-first wording.
  </action>
  <verify>pytest tests/test_alignment_templates.py tests/codex_team/test_implement_runtime_routing.py -q</verify>
  <acceptance_criteria>
    - `templates/commands/implement.md` contains `leader` language for the invoking runtime.
    - `templates/commands/implement.md` mentions selecting the next executable phase or equivalent milestone-ready step.
    - `templates/commands/implement.md` still contains `single-lane`, `native-multi-agent`, and `sidecar-runtime`.
    - `pytest tests/test_alignment_templates.py tests/codex_team/test_implement_runtime_routing.py -q` exits with code 0.
  </acceptance_criteria>
  <done>The shared implement contract and regression tests now encode leader-only scheduling behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "class PhaseExecutionState|class MilestoneExecutionDecision" src/specify_cli/orchestration/models.py` returns matches.
- [ ] `rg -n "def select_next_phase|def build_milestone_execution_decision" src/specify_cli/orchestration/scheduler.py` returns matches.
- [ ] `rg -n "leader|next executable phase|single-lane|native-multi-agent|sidecar-runtime" templates/commands/implement.md` returns matches.
- [ ] `pytest tests/test_alignment_templates.py tests/codex_team/test_implement_runtime_routing.py -q` passes.
</verification>

<success_criteria>
- All tasks completed
- The shared implement surface encodes a leader-only contract
- Scheduler primitives exist in the orchestration core
- Regression tests catch drift in leader-only wording and milestone scheduling language
</success_criteria>

<output>
After completion, create `.planning/phases/10-leader-contract-and-milestone-scheduler/10-01-SUMMARY.md`
</output>
