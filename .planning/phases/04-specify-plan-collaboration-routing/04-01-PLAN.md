---
phase: 04-specify-plan-collaboration-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/commands/specify.md
  - templates/commands/plan.md
  - tests/test_alignment_templates.py
  - tests/orchestration/test_policy.py
autonomous: true
requirements: [WF-01, WF-02, LANE-01]
must_haves:
  truths:
    - "`specify` documents the shared decision order before spec generation proceeds."
    - "`plan` documents the shared decision order before research and design fan-out proceeds."
    - "Both workflows describe their lane purposes and join points in a way that matches the multi-CLI collaboration design."
    - "Shared templates remain free of Codex-only `specify team` wording."
  artifacts:
    - path: "templates/commands/specify.md"
      provides: "Analysis workflow strategy-selection and lane-planning contract"
    - path: "templates/commands/plan.md"
      provides: "Planning workflow strategy-selection and lane-planning contract"
    - path: "tests/test_alignment_templates.py"
      provides: "Template-level contract coverage for shared workflow routing language"
    - path: "tests/orchestration/test_policy.py"
      provides: "Generic policy coverage proving the shared chooser works beyond `implement`"
  key_links:
    - from: "templates/commands/specify.md"
      to: "src/specify_cli/orchestration/policy.py"
      via: "shared strategy names, reason codes, and decision order"
    - from: "templates/commands/plan.md"
      to: "docs/superpowers/specs/2026-04-13-multi-cli-agent-collaboration-design.md"
      via: "workflow-specific lane purposes and join points"
---

<objective>
Extend the shared orchestration contract into the `specify` and `plan` workflows so they choose the same execution strategies as `implement`, describe their lane plans and join points explicitly, and remain truthful about integration surfaces.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-specify-plan-collaboration-routing/04-CONTEXT.md
@docs/superpowers/specs/2026-04-13-multi-cli-agent-collaboration-design.md
@src/specify_cli/orchestration/policy.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add strategy-selection and lane-planning language to `specify`</name>
  <files>templates/commands/specify.md</files>
  <action>
    - Add an explicit step that evaluates workload shape with the shared policy before capability decomposition.
    - Use the canonical strategy names and reason ordering from `src/specify_cli/orchestration/policy.py`.
    - Describe the `specify` candidate lanes and join points from the multi-CLI collaboration design.
    - Instruct the workflow to record the decision in `alignment.md` and keep shared template language free of Codex-only runtime terms.
  </action>
  <done>`specify` exposes the shared collaboration contract in planning-ready language.</done>
</task>

<task type="auto">
  <name>Task 2: Add strategy-selection and lane-planning language to `plan`</name>
  <files>templates/commands/plan.md</files>
  <action>
    - Add an explicit decision step before research and design fan-out.
    - Describe the `plan` candidate lanes and join points from the multi-CLI collaboration design.
    - Ensure plan output carries the execution decision and any fallback rationale into the generated planning artifacts.
    - Keep the shared template neutral with respect to Codex-only runtime surfaces.
  </action>
  <done>`plan` exposes the shared collaboration contract and planning-specific lane model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression coverage for `specify` and `plan` routing language</name>
  <files>tests/test_alignment_templates.py, tests/orchestration/test_policy.py</files>
  <action>
    - Add template assertions that `specify` and `plan` mention the canonical strategy names, decision order, lane purposes, and join points.
    - Add generic policy tests that call `choose_execution_strategy()` with `command_name="specify"` and `command_name="plan"` to prove the chooser remains command-agnostic.
    - Keep the tests aligned with existing contract-test style.
  </action>
  <verify>
    <automated>pytest tests/test_alignment_templates.py tests/orchestration/test_policy.py -q</automated>
  </verify>
  <done>Regression coverage locks the Phase 4 contract in place.</done>
</task>

</tasks>

<success_criteria>
1. `specify` selects and records a shared execution strategy before spec generation proceeds.
2. `plan` selects and records a shared execution strategy before design fan-out proceeds.
3. Both workflows document phase-appropriate lanes and join points from the approved design.
4. Shared workflow templates stay free of `specify team` wording.
</success_criteria>

<output>
After completion, create `.planning/phases/04-specify-plan-collaboration-routing/04-01-SUMMARY.md` and `.planning/phases/04-specify-plan-collaboration-routing/04-VERIFICATION.md`.
</output>
