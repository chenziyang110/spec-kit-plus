---
phase: 03-autonomous-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/specify_cli/debug/schema.py, src/specify_cli/debug/persistence.py, src/specify_cli/debug/graph.py, tests/test_debug_graph_nodes.py]
autonomous: true
requirements: [FND-04, SYS-01]
must_haves:
  truths:
    - "reproduction_verified is strictly enforced in GatheringNode before moving to Investigation"
    - "fail_count is tracked in the Resolution state for safety gate logic"
    - "The agent receives clear instructions in next_action when reproduction is missing"
  artifacts:
    - path: "src/specify_cli/debug/schema.py"
      provides: "Updated Resolution and Symptoms models"
    - path: "src/specify_cli/debug/persistence.py"
      provides: "Serialization for failure tracking"
    - path: "src/specify_cli/debug/graph.py"
      provides: "Enforced reproduction gate"
  key_links:
    - from: "src/specify_cli/debug/graph.py"
      to: "src/specify_cli/debug/schema.py"
      via: "state checks in GatheringNode"
---

<objective>
Refine the investigation state and enforce the "No Fix Without Proof" gate.
This ensures that the agent cannot proceed to fixing until a failing reproduction has been verified.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/specify_cli/debug/schema.py
@src/specify_cli/debug/graph.py
@src/specify_cli/debug/persistence.py
@tests/test_debug_graph_nodes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Schema and Persistence for Failure Tracking</name>
  <files>src/specify_cli/debug/schema.py, src/specify_cli/debug/persistence.py</files>
  <behavior>
    - Add `fail_count: int = 0` to `Resolution` model.
    - Add `reproduction_command: Optional[str] = None` to `Symptoms` model.
    - Update `persistence.py` to correctly serialize/deserialize these fields.
  </behavior>
  <action>
    - Modify `Resolution` class in `schema.py` to include `fail_count`.
    - Modify `Symptoms` class in `schema.py` to include `reproduction_command`.
    - Update `MarkdownPersistenceHandler.save` to write `fail_count` and `reproduction_command` to the markdown file.
    - Update `MarkdownPersistenceHandler.load` to read these values back.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py -k "test_persistence" (once updated)</automated>
  </verify>
  <done>Schema supports failure tracking and reproduction commands; persistence is verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Enforced Reproduction Gate</name>
  <files>src/specify_cli/debug/graph.py, tests/test_debug_graph_nodes.py</files>
  <behavior>
    - `GatheringNode` must return `self` if `reproduction_verified` is False.
    - If symptoms are provided but `reproduction_verified` is False, set `next_action` to prompt the agent to create and verify a reproduction script.
  </behavior>
  <action>
    - Refine `GatheringNode.run` in `graph.py` to check `ctx.state.symptoms.reproduction_verified`.
    - If False, set `ctx.state.current_focus.next_action = "Reproduction not verified. Please create a reproduction script and run it to verify the bug. Update symptoms.reproduction_verified to True once confirmed."`
    - Update `tests/test_debug_graph_nodes.py` to verify this behavior.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py -k "test_gathering_node"</automated>
  </verify>
  <done>Investigation cannot start without a verified reproduction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System -> Schema | Malformed persistence files could crash the debugger |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | persistence.py | mitigate | Use Pydantic validation during loading to ensure state integrity. |
</threat_model>

<success_criteria>
1. `sp-debug` session stays in GATHERING mode if `reproduction_verified` is False.
2. `fail_count` is correctly persisted in the debug markdown file.
</success_criteria>

<output>
After completion, create `.planning/phases/03-autonomous-resolution/03-01-SUMMARY.md`
</output>
