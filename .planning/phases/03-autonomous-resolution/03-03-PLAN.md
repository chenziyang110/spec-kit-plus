---
phase: 03-autonomous-resolution
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [src/specify_cli/debug/graph.py, src/specify_cli/debug/persistence.py, src/specify_cli/debug/cli.py]
autonomous: true
requirements: [TOL-02]
must_haves:
  truths:
    - "Safety Gate triggers AwaitingHumanNode after 3rd verification failure (fail_count > 2)"
    - "CLI stops processing when AWAITING_HUMAN status is reached"
    - "User receives a clear summary of all failed attempts during HITL"
  artifacts:
    - path: "src/specify_cli/debug/graph.py"
      provides: "AwaitingHumanNode and safety gate logic with > 2 threshold"
    - path: "src/specify_cli/debug/persistence.py"
      provides: "HITL report generation"
    - path: "src/specify_cli/debug/cli.py"
      provides: "Handling of AWAITING_HUMAN status and terminal output"
  key_links:
    - from: "src/specify_cli/debug/graph.py"
      to: "src/specify_cli/debug/persistence.py"
      via: "report generation for HITL"
    - from: "src/specify_cli/debug/cli.py"
      to: "src/specify_cli/debug/graph.py"
      via: "status-based loop termination"
---

<objective>
Implement the Human-in-the-Loop (HITL) safety gate to prevent infinite loops on difficult bugs, and update the CLI to handle the handoff report and pause execution for human intervention.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-autonomous-resolution/03-02-SUMMARY.md
@src/specify_cli/debug/graph.py
@src/specify_cli/debug/persistence.py
@src/specify_cli/debug/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Safety Gate with fail_count > 2 Threshold</name>
  <files>src/specify_cli/debug/graph.py</files>
  <action>
    - In `VerifyingNode.run`, check if `ctx.state.resolution.fail_count > 2` (per D6 - more than 2).
    - If threshold reached, transition to `AwaitingHumanNode` instead of `InvestigatingNode`.
    - Implement `AwaitingHumanNode.run`: Set session status to `AWAITING_HUMAN`, and provide high-level instructions for the human to review the investigation report.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py -k "test_safety_gate" (add test first verifying transition after 3rd failure)</automated>
  </verify>
  <done>Safety gate triggers AwaitingHumanNode exactly after the 3rd failure.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HITL Reporting</name>
  <files>src/specify_cli/debug/persistence.py, src/specify_cli/debug/graph.py</files>
  <action>
    - Add a method to `MarkdownPersistenceHandler` to generate a comprehensive "Session Summary" report (failed hypotheses, evidence, attempted fixes).
    - In `AwaitingHumanNode`, use this report generator to populate the session's final next action or a dedicated report field in the markdown.
  </action>
  <verify>
    <automated>grep "## Resolution" .planning/debug/test-session.md -A 20 (manually check output format in a test run)</automated>
  </verify>
  <done>The debugger provides a detailed failure audit trail for the human to review.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLI for HITL Handoff</name>
  <files>src/specify_cli/debug/cli.py</files>
  <action>
    - Update the `debug` command logic in `cli.py` to monitor the graph's status.
    - If status reaches `AWAITING_HUMAN`, stop the autonomous processing loop.
    - Output the session summary/report to the terminal (e.g., using rich for formatting).
    - Inform the user that the session is paused and provide the path to the debug markdown file for further manual investigation (TOL-02).
  </action>
  <verify>
    <automated>pytest tests/test_debug_cli.py -k "test_awaiting_human_status"</automated>
  </verify>
  <done>CLI correctly handles HITL handoff, stops execution, and presents the report.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full Phase 3 loop: Reproduction -> Investigation -> Fix -> Verify -> HITL Handoff</ what-built>
  <how-to-verify>
    1. Run `specify debug --new "hitl-test"`.
    2. Simulate reproduction and 3 consecutive verification failures.
    3. Verify that the CLI output shows the session summary and indicates "Awaiting Human Review".
    4. Check the generated markdown file for the complete audit trail.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Human -> CLI | User input after pause could be used for malicious purposes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Denial of Service | graph.py | mitigate | Safety gate prevents infinite loops and excessive resource/context consumption. |
</threat_model>

<success_criteria>
1. Debugger transitions to `AWAITING_HUMAN` after more than 2 failed verification attempts.
2. The CLI gracefully terminates and displays a detailed report to the user.
3. All attempted fixes and their outputs are recorded in the debug session markdown.
</success_criteria>

<output>
After completion, create `.planning/phases/03-autonomous-resolution/03-03-SUMMARY.md`
</output>
