---
phase: 03-autonomous-resolution
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/specify_cli/debug/graph.py, src/specify_cli/debug/utils.py, tests/test_debug_graph_nodes.py]
autonomous: true
requirements: [SYS-03, SYS-04, TOL-01]
must_haves:
  truths:
    - "FixingNode transitions to VerifyingNode once a fix is proposed"
    - "VerifyingNode autonomously runs reproduction and feature directory tests using run_command"
    - "Verification failures increment fail_count and return to investigation/fixing"
  artifacts:
    - path: "src/specify_cli/debug/utils.py"
      provides: "Command execution, file reading, and file editing helpers"
    - path: "src/specify_cli/debug/graph.py"
      provides: "Autonomous Fixing and Verifying node logic using utils"
  key_links:
    - from: "src/specify_cli/debug/graph.py"
      to: "src/specify_cli/debug/utils.py"
      via: "autonomous tool execution during fixing/verification"
---

<objective>
Implement the autonomous fixing and verification logic. Provide the agent with secure tools to read, edit, and execute code, and ensure the VerifyingNode autonomously validates fixes against the reproduction script and all tests in the feature directory.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-autonomous-resolution/03-01-SUMMARY.md
@src/specify_cli/debug/graph.py
@src/specify_cli/debug/utils.py
@tests/test_debug_graph_nodes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Execution Helpers</name>
  <files>src/specify_cli/debug/utils.py</files>
  <action>
    - Implement `run_command(cmd: str) -> str`: Uses `subprocess.run(shell=True, capture_output=True, text=True)` to execute commands and return combined stdout/stderr.
    - Implement `edit_file(path: str, content: str)`: Overwrites the file at `path` with `content`.
    - Implement `read_file(path: str) -> str`: Reads and returns file content.
    - Per TOL-01, these helpers provide the core capabilities for autonomous debugging.
  </action>
  <verify>
    <automated>python -c "from specify_cli.debug.utils import run_command; print(run_command('echo hello'))" | grep hello</automated>
  </verify>
  <done>Standardized tools for command execution, file reading, and editing are available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Autonomous Fixing and Verifying Node Logic</name>
  <files>src/specify_cli/debug/graph.py, tests/test_debug_graph_nodes.py</files>
  <behavior>
    - `FixingNode`: If `resolution.fix` is set (proposed code changes), transition to `VerifyingNode`. Otherwise, prompt agent for fix in `next_action`.
    - `VerifyingNode`: Autonomously execute `reproduction_command` AND all pytest tests in the current feature's directory (per D2).
    - Use `run_command` for all executions.
    - If ALL tests pass: Set `resolution.verification = 'success'`, move to `ResolvedNode`.
    - If ANY test fails: Increment `fail_count`, set `resolution.verification = 'failed'`, move back to `InvestigatingNode`.
  </behavior>
  <action>
    - Update `FixingNode.run` in `graph.py` to handle transition.
    - Update `VerifyingNode.run` in `graph.py` to perform autonomous verification logic using `run_command`.
    - Logic should identify the "feature directory" from the file being fixed or the current session context.
    - Update `tests/test_debug_graph_nodes.py` to verify autonomous execution and failure handling.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py -k "test_fixing_node or test_verifying_node"</automated>
  </verify>
  <done>Autonomous fix-verify-fail loop is implemented; VerifyingNode runs reproduction and directory tests.</done>
</task>

<task type="auto">
  <name>Task 3: Expose Helpers to Agent/CLI</name>
  <files>src/specify_cli/debug/graph.py, src/specify_cli/debug/utils.py</files>
  <action>
    - Ensure the `DebugGraphState` or the nodes have direct access to `run_command`, `edit_file`, and `read_file`.
    - Per TOL-01, expose these utilities so the agent can invoke them as part of its available toolset during the Fixing and Investigation phases.
    - This might involve passing them as dependencies to the graph or making them available in the node's context.
  </action>
  <verify>
    <automated>pytest tests/test_debug_graph_nodes.py -k "test_tool_access"</automated>
  </verify>
  <done>Utility helpers are properly exposed and accessible for autonomous operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Tool Execution -> OS | Running arbitrary commands and editing files can be dangerous |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Elevation of Privilege | utils.py | accept | The agent is trusted to run commands in the local dev environment. Mitigation: All tool executions and their outputs are logged in the debug markdown file for auditability. |
</threat_model>

<success_criteria>
1. `FixingNode` moves to `VerifyingNode` when a fix is provided.
2. `VerifyingNode` autonomously runs the reproduction script and all tests in the feature directory.
3. Verification failures increment the failure counter and trigger a return to investigation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-autonomous-resolution/03-02-SUMMARY.md`
</output>
