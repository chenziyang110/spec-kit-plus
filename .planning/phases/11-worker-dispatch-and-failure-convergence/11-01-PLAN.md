---
phase: 11-worker-dispatch-and-failure-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/specify_cli/orchestration/models.py
  - src/specify_cli/orchestration/policy.py
  - src/specify_cli/orchestration/scheduler.py
  - src/specify_cli/codex_team/runtime_state.py
  - src/specify_cli/codex_team/auto_dispatch.py
  - src/specify_cli/codex_team/batch_ops.py
  - src/specify_cli/codex_team/task_ops.py
  - tests/orchestration/test_policy.py
  - tests/codex_team/test_passive_parallelism.py
  - tests/codex_team/test_auto_dispatch.py
autonomous: true
requirements: [ORCH-03, LEAD-03]
user_setup: []
must_haves:
  truths:
    - "Safe parallel batches dispatch multiple worker lanes under explicit join-point semantics before dependent work continues."
    - "Limited safe cross-phase preparation is allowed only for low-risk preparation work and must not violate roadmap-order truth."
    - "Batch and join-point state is machine-readable in the runtime rather than implied only by docs or task prose."
  artifacts:
    - path: "src/specify_cli/orchestration/policy.py"
      provides: "Shared policy for classifying batches and deciding whether safe cross-phase preparation is allowed."
    - path: "src/specify_cli/codex_team/auto_dispatch.py"
      provides: "Codex-team dispatch behavior that attaches batch and join-point policy metadata to worker lanes."
    - path: "tests/codex_team/test_auto_dispatch.py"
      provides: "Regression coverage for dispatch metadata and join-point convergence behavior."
  key_links:
    - from: "src/specify_cli/orchestration/policy.py"
      to: "src/specify_cli/codex_team/auto_dispatch.py"
      via: "Shared batch classification and safe-preparation decisions must flow into runtime dispatch."
    - from: "src/specify_cli/codex_team/batch_ops.py"
      to: "tests/codex_team/test_auto_dispatch.py"
      via: "Join-point convergence semantics must be enforced by runtime code and proven by tests."
---

<objective>
Implement worker dispatch, join-point coordination, and safe cross-phase preparation rules for Phase 11.

Purpose: Extend the existing leader-only runtime so it can dispatch parallel work safely, represent join points explicitly, and allow only narrowly-scoped later-phase preparation without breaking roadmap-order truth.
Output: Shared policy/state primitives plus Codex-team dispatch and join-point behavior that distinguishes strict vs mixed-tolerance batch handling.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-worker-dispatch-and-failure-convergence/11-CONTEXT.md
@.planning/phases/11-worker-dispatch-and-failure-convergence/11-RESEARCH.md
@.planning/phases/10-leader-contract-and-milestone-scheduler/10-VERIFICATION.md
@AGENTS.md
@src/specify_cli/orchestration/models.py
@src/specify_cli/orchestration/policy.py
@src/specify_cli/orchestration/scheduler.py
@src/specify_cli/codex_team/runtime_state.py
@src/specify_cli/codex_team/auto_dispatch.py
@src/specify_cli/codex_team/batch_ops.py
@src/specify_cli/codex_team/task_ops.py
@tests/orchestration/test_policy.py
@tests/codex_team/test_passive_parallelism.py
@tests/codex_team/test_auto_dispatch.py
</context>

<threat_model>
<trust_boundaries>
- Shared orchestration policy -> Codex-team dispatch runtime -> persisted batch and join-point state
- Roadmap-order contract -> safe cross-phase preparation rules -> later-phase work eligibility
- Join-point metadata -> downstream task readiness -> milestone truthfulness
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>P11-01-T1</threat_id>
    <category>Tampering</category>
    <component>safe cross-phase preparation</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Encode low-risk preparation eligibility explicitly in shared policy and batch metadata so later-phase work cannot silently become broad out-of-order implementation.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>P11-01-T2</threat_id>
    <category>Repudiation</category>
    <component>join-point convergence</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist batch classification and join-point status transitions in runtime state and verify them with dispatch tests.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression coverage for batch classification, safe preparation, and join-point policy</name>
  <files>tests/orchestration/test_policy.py, tests/codex_team/test_passive_parallelism.py, tests/codex_team/test_auto_dispatch.py</files>
  <read_first>tests/orchestration/test_policy.py, tests/codex_team/test_passive_parallelism.py, tests/codex_team/test_auto_dispatch.py, .planning/phases/11-worker-dispatch-and-failure-convergence/11-CONTEXT.md, .planning/phases/11-worker-dispatch-and-failure-convergence/11-RESEARCH.md</read_first>
  <action>
    Extend `tests/orchestration/test_policy.py` so the shared orchestration layer must expose explicit batch classification and safe-preparation decisions for Phase 11. Extend `tests/codex_team/test_passive_parallelism.py` so enhancement-stage passive parallelism only triggers for low-risk preparation lanes and rejects broad later-phase implementation lanes. Extend `tests/codex_team/test_auto_dispatch.py` so dispatched batch records and join-point metadata must include the selected batch classification and safe-preparation policy details for `Parallel Batch 1.1`.
  </action>
  <verify>pytest tests/orchestration/test_policy.py tests/codex_team/test_passive_parallelism.py tests/codex_team/test_auto_dispatch.py -q</verify>
  <acceptance_criteria>
    - `tests/orchestration/test_policy.py` contains assertions for batch classification or safe-preparation decision data.
    - `tests/codex_team/test_passive_parallelism.py` contains assertions that broad later-phase implementation lanes do not qualify as safe preparation.
    - `tests/codex_team/test_auto_dispatch.py` contains assertions that batch or join-point metadata records the selected batch classification.
    - `pytest tests/orchestration/test_policy.py tests/codex_team/test_passive_parallelism.py tests/codex_team/test_auto_dispatch.py -q` exits with code 0 after implementation.
  </acceptance_criteria>
  <done>Phase 11 policy changes are locked behind regression coverage before runtime code is updated.</done>
</task>

<task type="auto">
  <name>Task 2: Extend shared orchestration policy and scheduler primitives for classified batches and limited safe preparation</name>
  <files>src/specify_cli/orchestration/models.py, src/specify_cli/orchestration/policy.py, src/specify_cli/orchestration/scheduler.py</files>
  <read_first>src/specify_cli/orchestration/models.py, src/specify_cli/orchestration/policy.py, src/specify_cli/orchestration/scheduler.py, .planning/ROADMAP.md, .planning/phases/11-worker-dispatch-and-failure-convergence/11-CONTEXT.md, .planning/phases/11-worker-dispatch-and-failure-convergence/11-RESEARCH.md</read_first>
  <action>
    Add explicit Phase 11 shared decision primitives so the orchestration layer can distinguish strict batches from mixed-tolerance batches and can represent whether a lane is allowed as low-risk safe preparation. Extend `src/specify_cli/orchestration/models.py` with concrete types or fields for batch classification and safe-preparation decisions. Extend `src/specify_cli/orchestration/policy.py` with helpers that classify a batch as `strict` or `mixed_tolerance`, decide whether preparation work is allowed only for low-risk lanes, and continue rejecting overlapping write sets. Extend `src/specify_cli/orchestration/scheduler.py` so later-phase preparation decisions remain subordinate to roadmap order and never imply early phase completion.
  </action>
  <verify>rg -n "strict|mixed_tolerance|safe preparation|safe_preparation|prepare|batch classification" src/specify_cli/orchestration/models.py src/specify_cli/orchestration/policy.py src/specify_cli/orchestration/scheduler.py</verify>
  <acceptance_criteria>
    - `src/specify_cli/orchestration/models.py` defines machine-readable batch classification or safe-preparation state.
    - `src/specify_cli/orchestration/policy.py` contains logic that distinguishes strict vs mixed-tolerance handling and limits safe preparation to low-risk lanes.
    - `src/specify_cli/orchestration/scheduler.py` contains roadmap-order language or code that keeps safe preparation subordinate to normal phase sequencing.
    - Existing execution-strategy behavior (`single-lane`, `native-multi-agent`, `sidecar-runtime`) remains present in `src/specify_cli/orchestration/policy.py`.
  </acceptance_criteria>
  <done>The shared orchestration layer now expresses the Phase 11 dispatch and preparation rules explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Apply classified dispatch and join-point coordination in the Codex-team runtime</name>
  <files>src/specify_cli/codex_team/runtime_state.py, src/specify_cli/codex_team/auto_dispatch.py, src/specify_cli/codex_team/batch_ops.py, src/specify_cli/codex_team/task_ops.py</files>
  <read_first>src/specify_cli/codex_team/runtime_state.py, src/specify_cli/codex_team/auto_dispatch.py, src/specify_cli/codex_team/batch_ops.py, src/specify_cli/codex_team/task_ops.py, src/specify_cli/orchestration/policy.py, .planning/phases/11-worker-dispatch-and-failure-convergence/11-RESEARCH.md</read_first>
  <action>
    Extend `src/specify_cli/codex_team/runtime_state.py` so batch records and join-point/task metadata can persist the selected batch classification, safe-preparation decision, and richer join-point status values. Update `src/specify_cli/codex_team/auto_dispatch.py` so routed batches attach that policy metadata when workers are dispatched. Update `src/specify_cli/codex_team/batch_ops.py` so strict batches only converge when every task succeeds, while mixed-tolerance batches can record a terminal but partially-satisfied join point without falsely marking blocked dependent work as ready. Update `src/specify_cli/codex_team/task_ops.py` so join-point entries preserve the classification and convergence status alongside task terminal transitions.
  </action>
  <verify>pytest tests/orchestration/test_policy.py tests/codex_team/test_passive_parallelism.py tests/codex_team/test_auto_dispatch.py tests/contract/test_codex_team_auto_dispatch_cli.py -q</verify>
  <acceptance_criteria>
    - `src/specify_cli/codex_team/runtime_state.py` persists batch classification and safe-preparation metadata in batch or task/join-point records.
    - `src/specify_cli/codex_team/auto_dispatch.py` writes the selected batch classification into dispatched batch metadata.
    - `src/specify_cli/codex_team/batch_ops.py` contains distinct convergence behavior for strict vs mixed-tolerance batches.
    - `pytest tests/orchestration/test_policy.py tests/codex_team/test_passive_parallelism.py tests/codex_team/test_auto_dispatch.py tests/contract/test_codex_team_auto_dispatch_cli.py -q` exits with code 0.
  </acceptance_criteria>
  <done>The Codex-team runtime now dispatches and converges worker batches using explicit Phase 11 policy rather than binary all-or-nothing behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "strict|mixed_tolerance|safe_preparation|prepare" src/specify_cli/orchestration/models.py src/specify_cli/orchestration/policy.py src/specify_cli/orchestration/scheduler.py` returns matches.
- [ ] `rg -n "batch_classification|safe_preparation|join_point" src/specify_cli/codex_team/runtime_state.py src/specify_cli/codex_team/auto_dispatch.py src/specify_cli/codex_team/batch_ops.py src/specify_cli/codex_team/task_ops.py` returns matches.
- [ ] `pytest tests/orchestration/test_policy.py tests/codex_team/test_passive_parallelism.py tests/codex_team/test_auto_dispatch.py tests/contract/test_codex_team_auto_dispatch_cli.py -q` passes.
</verification>

<success_criteria>
- All tasks completed
- Safe parallel dispatch is coordinated under explicit join-point semantics
- Limited safe cross-phase preparation stays subordinate to roadmap-order truth
- Batch and join-point policy is machine-readable and regression-tested
</success_criteria>

<output>
After completion, create `.planning/phases/11-worker-dispatch-and-failure-convergence/11-01-SUMMARY.md`
</output>
