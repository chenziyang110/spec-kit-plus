---
phase: 08-guided-follow-up-experience
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - templates/commands/specify.md
  - tests/test_alignment_templates.py
autonomous: true
requirements: [EXPQ-03]
user_setup: []
must_haves:
  truths:
    - "Common docs/config/process-change flows can reach planning-ready alignment inside `sp-specify` without requiring `sp.clarify` or `sp.spec-extend`."
    - "The confirmation step reads like a real pre-release check rather than a generic recap."
    - "Regression tests fail if the no-redirect guided-flow contract drifts."
  artifacts:
    - path: "templates/commands/specify.md"
      provides: "The common-flow and confirmation-step contract for guided specify runs."
    - path: "tests/test_alignment_templates.py"
      provides: "Regression coverage for no-redirect guided-flow expectations."
  key_links:
    - from: "templates/commands/specify.md"
      to: "tests/test_alignment_templates.py"
      via: "Experience-validation assertions for confirmation behavior and no-redirect expectations."
    - from: "templates/commands/specify.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 8 coverage for EXPQ-03."
---

<objective>
Validate the real `sp-specify` interaction experience against milestone expectations by locking common-flow and no-redirect behavior into the shared contract.

Purpose: Ensure the guided interaction model actually supports milestone target flows instead of only sounding better in isolated lines.
Output: Refined template language plus regression checks for confirmation quality and staying inside `sp-specify`.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-guided-follow-up-experience/08-CONTEXT.md
@.planning/phases/08-guided-follow-up-experience/08-RESEARCH.md
@.planning/phases/08-guided-follow-up-experience/08-01-SUMMARY.md
@templates/commands/specify.md
@tests/test_alignment_templates.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing assertions for common-flow completion inside `sp-specify`</name>
  <files>tests/test_alignment_templates.py</files>
  <read_first>tests/test_alignment_templates.py, templates/commands/specify.md, .planning/REQUIREMENTS.md</read_first>
  <action>
    Extend the `specify` contract test with assertions that common docs/config/process-change flows can reach planning-ready alignment inside `sp-specify`, that the confirmation step is an explicit pre-release check, and that the mainline does not send ordinary runs to `sp.clarify` or `sp.spec-extend`. Keep the assertions phrase-based and specific enough to fail before template edits.
  </action>
  <verify>pytest tests/test_alignment_templates.py -q</verify>
  <acceptance_criteria>
    - `tests/test_alignment_templates.py` contains new assertions for no-redirect guided-flow expectations.
    - `pytest tests/test_alignment_templates.py -q` fails before the new template wording lands.
  </acceptance_criteria>
  <done>The regression suite now demands the Phase 8 common-flow experience contract.</done>
</task>

<task type="auto">
  <name>Task 2: Refine the confirmation and no-redirect wording in `specify`</name>
  <files>templates/commands/specify.md</files>
  <read_first>templates/commands/specify.md, .planning/REQUIREMENTS.md, .planning/phases/08-guided-follow-up-experience/08-CONTEXT.md, .planning/phases/08-guided-follow-up-experience/08-RESEARCH.md</read_first>
  <action>
    Refine the guided interaction contract so the confirmation step reads like an explicit pre-release check, and make the target docs/config/process-change flows clearly able to finish inside `sp-specify` when ambiguity is resolved. Preserve the compatibility-only framing for `sp.clarify` and the optional enhancement framing for `sp.spec-extend`.
  </action>
  <verify>rg -n "pre-release check|inside `sp-specify`|`/sp.clarify` for compatibility-only|`/sp.plan` for the mainline|`spec-extend` as the optional enhancement path" templates/commands/specify.md</verify>
  <acceptance_criteria>
    - `templates/commands/specify.md` makes the confirmation step a clear pre-release check.
    - `templates/commands/specify.md` states that common target flows can finish inside `sp-specify` once ambiguity is resolved.
    - `templates/commands/specify.md` keeps `sp.clarify` compatibility-only and `spec-extend` optional.
  </acceptance_criteria>
  <done>The shared template now expresses the common-flow experience expected for Phase 8.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the common-flow experience regression suite</name>
  <files>tests/test_alignment_templates.py</files>
  <read_first>tests/test_alignment_templates.py, templates/commands/specify.md</read_first>
  <action>
    Run the focused regression suite and make only the minimal follow-up adjustments needed so the common-flow and confirmation-quality assertions pass.
  </action>
  <verify>pytest tests/test_alignment_templates.py -q</verify>
  <acceptance_criteria>
    - `pytest tests/test_alignment_templates.py -q` exits with code 0.
    - The new common-flow and no-redirect assertions remain in the test file.
  </acceptance_criteria>
  <done>The common-flow experience contract is protected by passing regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_alignment_templates.py -q` passes.
- [ ] `rg -n "pre-release check|inside `sp-specify`|compatibility-only|optional enhancement path" templates/commands/specify.md` returns matches.
- [ ] The guided flow still keeps `specify -> plan` as the mainline.
</verification>

<success_criteria>
- All tasks completed
- The target docs/config/process-change flows can complete inside `sp-specify`
- The confirmation gate reads like an intentional pre-release check
- Regression tests protect EXPQ-03
</success_criteria>

<output>
After completion, create `.planning/phases/08-guided-follow-up-experience/08-02-SUMMARY.md`
</output>
