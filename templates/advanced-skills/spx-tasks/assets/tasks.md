# Tasks: {{feature_name}}

Use stable IDs such as `T001`.

## Execution Order

- [ ] T001 Outcome — paths; dependencies; acceptance; verification

For UI-bearing tasks, follow each checklist line with the exact detail structure
from `assets/ui-task.md` and mirror its structured fields into `task-index.json`.

## Parallel Batches and Join Points

## Requirement and Obligation Traceability

## UI Contract Coverage

- UI task IDs and source `ui-brief.md`/`ui_design_contract` refs:
