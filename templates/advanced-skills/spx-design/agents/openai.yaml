interface:
  display_name: "SPX Design"
  short_description: "Create or audit the root design system"
  default_prompt: "Use $spx-design to create or revise the repository's DESIGN.md contract."

policy:
  allow_implicit_invocation: false
